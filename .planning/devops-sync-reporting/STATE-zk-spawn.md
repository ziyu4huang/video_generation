# zk-spawn state — branch `devops-sync-reporting` (pushed)

Session interrupted by token budget. Nothing implemented yet. Read this file first, then the ground-truth in the task prompt (still accurate).

## Done

1. Branch `devops-sync-reporting` created off `origin/main` via `bun bun-apps/pi-agent-ext-devops/src/prepare-cli.ts --branch devops-sync-reporting --create --repo-root /Users/huangziyu/proj/video_generation__webui` and pushed with upstream (`-u`). Working tree still has unrelated dirty files (`.agents/memory/MEMORY.md`, `vaults_root/study-news` submodule) — leave them.
2. Read all key sources: `src/sync-recipe.ts` (479 L), `src/gh.ts` (434 L), `src/prepare-recipe.ts`, `src/pr-finish-cli.ts`, `src/branch-recipe.ts` (BranchClient), `extensions/devops.ts`, `tests/{sync-recipe,sync-cli,prepare-recipe,pr-finish-cli,gh}.test.ts`, `tsconfig.json`, `package.json`.
3. tsconfig widening PROBED (temp `tsconfig.probe.json` inside the pkg dir, since deleted). Findings below.
4. Grepped `bun-apps/` for external consumers of `submodules[].clean` / `advanced[]` shapes: **NONE outside pi-agent-ext-devops** (only skill md mentions in superpowers, unrelated). So #2 shape change is package-local.

## Key ground truth (verified against current code)

- sync-recipe.ts full-mode flow: detect D (`defaultBranch` → fallback "main"), pre-flight ahead warnings, worktree-aware `advanceTarget` (worktree holding D, else repoRoot+checkout), preserve stash park/pop, ff-only vs force reset, step-8 verify (revParse D vs `to`, warning only, gated `if (!dry)`), then 4 submodule cmds at repoRoot only, `parseSubmoduleStatus(status.stdout)` → `{path, sha, clean: s.flag === ""}`.
- `SyncClient = Pick<BranchClient, "defaultBranch"|"currentBranch"|"worktreeList"|"revParse"|"dirtyPaths"|"aheadBehind">` — for #1 caller post-state needs `aheadBehind(base=origin/D, head=callerBranch)`; detached detection: `currentBranch()` returns `"HEAD"` when detached (client impl: rev-parse --abbrev-ref HEAD → "HEAD"). NOTE: task said `aheadBetween` doesn't exist — correct, use `aheadBehind("origin/main", "feat/x")` and read `.behind`.
- `outcome()` closure currently returns fixed shape; add `caller`, `verification` fields there (declare `let caller: ... | undefined; let verification: ... | undefined;` before closure like `preserved`).
- abort reasons emitted in sync-recipe: `aborted_before_start`, `dirty_tree`, `no_origin_ref`, `checkout_failed`, `preserve_failed` (×2, full + rebase/pull), `divergent`, `reset_failed`, `detached_head`, `rebase_failed`, `merge_failed`. JSDoc at SyncAbort.reason (line ~131) is stale (documents `merge_ff_failed`, omits `preserve_failed`, `rebase_failed`, `merge_failed`, `aborted_before_start`). For #8-code: export `SYNC_ABORT_REASONS` with the SPEC's exact set — spec says: divergent, dirty_worktree?? NO — spec literally: `divergent, dirty_worktree, force_required, merge_target_dirty, merge_target_divergent, preserve_failed, rebase_conflict, detached_head` — **VERIFY against actual emissions; the spec set does NOT match actual emissions** (actual uses `dirty_tree` not `dirty_worktree`; `force_required`/`merge_target_*`/`rebase_conflict` are NOT emitted by sync; prepare uses hyphenated `rebase-conflict`). Spec says "exact emitted set … VERIFY against actual emissions incl. prepare's new one if shared" → the list in the spec looks copy-pasted from another recipe (recipe.ts runMergeRecipe?). CHECK `src/recipe.ts` for those reason strings before deciding; the safest reading: SYNC_ABORT_REASONS = actual emitted set of sync-recipe + prepare's new `detached-head`?? Task #6 says prepare abort reason string is `detached-head` (hyphenated, consistent with prepare's other reasons like `worktree-conflict`). Decision: export the union of what sync-recipe actually emits + prepare's existing reasons via PrepareAbort's typed union (already typed!). For sync: type `reason: (typeof SYNC_ABORT_REASONS)[number]` with actual emitted literals. Document deviation in report/PR body: spec's literal list didn't match reality; used actual emissions (dirty_tree, preserve_failed, etc.).
- prepare-recipe.ts: `branch = opts.branch ?? current` — detached → current = "" → flows into `checkout -b ""`. Guard #6: after resolving branch+base, `if (!branch) return outcome({aborted:true, reason:"detached-head", message:..., hint: pass --branch})`. Add `head: {from, to}` (revParse HEAD before/after rebase; only meaningful when rebase ran — also fine always when steps ran) + post-rebase `aheadBehind(base, branch)`... PrepareClient lacks aheadBehind — need to widen PrepareClient Pick with `aheadBehind` (and tests' fakes + extension passes full client anyway; prepare-cli uses createBranchClient — fine). Outcome fields: `head?: {from: string; to: string}`, `aheadBehind?: {ahead: number; behind: number}` (vs base).
- pr-finish-cli recordingSpawn (line ~112-120): `commands.push([cmd, ...args].join(" "))` — for git spawns record `git -C <options.cwd|repoRoot> args` form. SpawnFn signature: `(cmd, args, options)` where options has `cwd`. Caveat: gh spawns (`gh pr merge ...`) have no cwd semantics — only rewrite when `cmd === "git"` and options?.cwd present: `[cmd, "-C", cwd, ...args].join(" ")`. Old form: e.g. `git branch -D feature` (from client.deleteLocalBranch which spawns without -C since createBranchClient methods don't pass -C... check: deleteLocalBranch spawns `git branch -D name` no -C). New: `git -C /repo branch -D feature`. Tests in pr-finish-cli.test.ts pin old planned-command strings (dry-run block pushes planned strings like `git branch -D feature` manually — those are hand-built `planned` array, may keep or update). Post-merge hint (#7a): after `gh.mergeNow` success (step 4), push warning: e.g. `default branch advanced by the merge — this worktree / the default-branch worktree may now be behind; run sync_repo (full mode) to catch up`. Cheap: unconditional after merge success.
- pr-finish `runPrFinishCli` has no per-step success hook — add warning right after the merge try block success.
- tests/pr-finish-cli.test.ts greenDeps fake client lacks `aheadBehind`/`worktreeList` extra methods — currently casts `as unknown as BranchClient` so adding methods to BranchClient won't break it; but prepare-recipe tests' fakeClient typed as PrepareClient WILL break if PrepareClient gains aheadBehind → add stub to fakeClient in tests/prepare-recipe.test.ts.
- sync-cli.test.ts fakeClient builds a full BranchClient-ish object via `base as unknown as BranchClient` — adding `logSubjects` to BranchClient interface will NOT break it (cast). But sync-recipe SyncClient doesn't need logSubjects; recipe calls it directly on opts.client which is SyncClient — for #5 add `logSubjects` to the SyncClient Pick (fake clients in sync-recipe.test.ts + sync-cli.test.ts must gain it → test updates).
- gh.ts createBranchClient: add `logSubjects(from, to, limit)`: `git log --format=%s from..to -n limit` → stdout lines filter non-empty. Under dryRun the recipe just doesn't call spawn — client is injected; runSync guards with `if (!dry)`? Spec: "under dryRun return [] and count: 0" — simplest: in recipe, only call logSubjects when !dry, set count via aheadBetween?? No — count should be git rev-list count... spec: `advanced[]` gets `count: number` + `subjects: string[]` (cap 15, `"... and N more"` if >15). count = ? "git log subjects from→to, cap 15" and count separate → count = actual number of commits (use client.aheadBehind(from, to).ahead? For ff-advance from..to ahead = N commits). Simpler: count = subjects.length would collapse the >15 case. Use `aheadBetween`... doesn't exist. Use `aheadBehind(to, from).ahead` (commits in `to` not in `from` = N advanced). Under dryRun: subjects [], count 0. Decide: count = aheadBehind(`${to}`, `${from}`).ahead? careful order: aheadBehind(base, head) → head..base? impl: ahead = rev-list count `base..head`. So aheadBetween(from,to) = aheadBehind(from, to).ahead = commits reachable to not from = N. Good.
- extensions/devops.ts formatSync (~line 122-147): submodules line `const dirty = o.submodules.filter((s) => !s.clean)` — rewrite for new shape: render per-worktree grouping + flag semantics; no "not-clean" phrasing. Also formatPrepare: optionally surface head from/to + ahead/behind (nice-to-have; keep minimal).
- entry.test.ts reads extensions/devops.ts exports (PI_*_PROBES) — unaffected.

## tsconfig widening findings (probed)

Current: include `src/**`, rootDir src, `types:["bun"]`, module Node16, declaration+sourceMap+outDir (but check = tsc --noEmit; no build script uses it → safe to drop rootDir/outDir/declaration/sourceMap? Keep config emitting-irrelevant since check is noEmit; but don't be gratuitous — set noEmit true and drop rootDir is cleanest; verify `bun run check` = `tsc --noEmit` (no -p flag → uses tsconfig.json)).

Probe (include src+tests+scripts+extensions, rootDir ".") produced these error groups:
1. **extensions/devops.ts — `gating` not on ToolDefinition (10 errors)**: upstream d.ts lacks `gating`. REPO CANONICAL FIX (obsidian/krea2/research-tool/tool-gate tsconfigs): add `"types": ["bun", "@repo/pi-agent-ext-core-interface"]` (gating module augmentation via `src/tool-gating.d.ts`) + devDependency `"@repo/pi-agent-ext-core-interface": "workspace:*"` (obsidian has exactly this). Needs `bun add` inside bun-apps (workspace) or manual package.json edit + `bun install` from bun-apps/.
2. **scripts/deploy.ts**: `javascript-obfuscator` module missing (optional runtime dep of deploy, imported dynamically) → fix via `await import(...)` typing as any is ugly; check how other pkgs handle: likely devDependency absent on purpose (obfuscate opt-in). Options: add `// @ts-expect-error` (single, justified) or declare module shim in scripts. Note in report if needed.
   `new Response(proc.stdout).text()` ×2 + implicit any `l` (line 639) — Response from Bun global: fixing requires lib DOM OR types resolution; @types/bun SHOULD provide Response (it does globally in bun types — but with moduleResolution Node16 + types:["bun"] the probe still errored?? Because `Response` in lib.dom vs bun-types ambient; obsidian adds "DOM" to lib. Try lib: ["ES2022","DOM"] first — likely fixes Response + .text()). `l` implicit any → add annotation.
3. **scripts/lib/build-extensions.ts + codegen.ts — TS6059 not under rootDir**: they import OUTSIDE the package (`../pi-agent/run-dir/manifest-types.ts`, `../pi-agent/scripts/generate-embedded-assets.ts`). With rootDir "." (pkg) still outside → error persists. FIX: DROP rootDir entirely (no rootDir) — TS6059 only fires when rootDir set. Also these cross-package imports use `.ts` extension + allowImportingTsExtensions ✓. But dropping rootDir with declaration/outDir... we run --noEmit only → drop rootDir + declaration + sourceMap + outDir, add noEmit:true. Verify pi-agent files don't then get pulled into the program graph beyond typechecking (they will be checked as part of program — that's FINE/desired? They belong to another package; may surface THEIR errors. Risk: cascade. If cascade too big, exclude those two files via "exclude" and note in report.) Probe showed only TS6059 for them (with rootDir set); after dropping rootDir re-probe for new errors from pi-agent files.
4. **scripts/lib/deploy-target-guard.test.ts** Response ×3 → DOM lib fix.
5. **tests/branch-recipe.test.ts(37)** fake BranchClient missing methods → add `worktreeList/revParse/isClean/dirtyPaths/aheadBehind` stubs to its fake (async () => undefined/[]/false/{}).
6. **tests/ci-recipe.test.ts(78), tests/recipe.test.ts(98,103)** implicit any `c`/`a` params → annotate `(c: ..., a: ...)`.
7. **tests/deploy-run.test.ts(35)** `ProcessEnv` not `ResolveOpts` — looks like wrong arg position (env passed where resolve opts expected?) → inspect & fix (probably cast or reorder args of `resolve()`).
8. **tests/entry.test.ts** accesses `.description`/`.gating` off tool defs — will be fixed by the same `types` + core-interface augmentation (gating) IF entry.test imports the extension file (it reads exported probes + maybe parses source? errors were property access → typed objects → augmentation fixes). Re-check after types fix.
9. **tests/main-health-recipe.test.ts(57) + tests/pr-finish-cli.test.ts(100)** CiOutcome missing `budgetMs, overBudget, slowest` → add to those ciPass fixtures. pr-finish-cli.test.ts(118) `runLocalCi` not imported → add import from "../src/ci-recipe.js"; (125,302) implicit any opts/cmd/args/options → annotate.
10. **tests/sync-cli.test.ts(89)** `expect(m).toBe(m)` string vs SyncMode — `if (r.ok) expect(r.args.mode).toBe(m);` with m: string → cast `m as SyncMode` or type the loop array as SyncMode[].

ALSO my own new code must keep these clean: annotate fake-client params, no implicit any.

## Implementation plan (ordered, all in bun-apps/pi-agent-ext-devops)

Commit 1 — feat(sync): caller post-state + verification snapshot + advanced enrichment + submodule semantics + advanceTarget submodules (#1-#5):
- src/gh.ts: `logSubjects` on BranchClient + createBranchClient + SubmoduleStatus doc tweak (flag semantics); parseSubmoduleStatus unchanged behavior (already unquotes shell-escaped).
- src/sync-recipe.ts:
  - SyncAdvanced += `count: number; subjects: string[]` (computed after advance: subjects = !dry ? await client.logSubjects(from,to,15) : [], count = !dry ? (await client.aheadBehind(from,to)).ahead : 0; if count>15 subjects.push(`... and ${count-15} more`)).
  - SyncSubmodule → `{ worktree, path, sha, flag: " "|"+"|"-"|"U", matchesRecordedGitlink: boolean }` (flag === " " → matchesRecordedGitlink true; also "+"/"-" → false; "U" false). Drop `clean`.
  - Subroutine `submoduleSync(dir)` issuing the 4 cmds at `dir`; full mode: run at repoRoot AND (if advanceTarget !== repoRoot) at advanceTarget with per-failure warn+continue (check each exitCode !== 0 → warnings.push, continue; status parse per worktree; tag entries with worktree).
  - Step 8 → `verification = { branch: D, local, remote: to, ok: local===to }` ALWAYS in full mode (dryRun: local = from? under dry local revParse would return real current — spec "always present in full mode outcome"; compute even in dry via revParse D (read-only, fine); ok accordingly; keep mismatch warning only when !dry per existing behavior — or always warn on mismatch; keep existing gate to avoid noisy dry warnings).
  - `caller` post-state: after everything (full + rebase/pull): branch = client.currentBranch() (fresh call post-op), detached = !branch || branch === "HEAD", behindDefault = detached ? null : (await client.aheadBehind(`origin/${D}`, branch)).behind; caller = { worktree: repoRoot, branch: detached ? null : branch, detached, behindDefault }; if (behindDefault ?? 0) > 0 → warnings.push(`calling worktree ${repoRoot} is ${N} commit(s) behind ${D} — full mode advances the default branch only in the worktree that holds it`). Compute in a helper so both modes get it (spec: full mandatory, rebase/pull "if trivial" — it is trivial, do both; note in report).
- extensions/devops.ts formatSync: new submodule rendering (group by worktree; flag legend: ' '=matches gitlink, '+'=drifted from gitlink, '-'=not initialized, 'U'=conflict); caller + verification lines; advanced line += `(N commits)`.
- Tests: gh.test.ts += describe("parseSubmoduleStatus") — flags ' ','+','-','U', quoted path unescape (`\"` etc), CRLF line endings, garbage skip. sync-recipe.test.ts: update fakeClient (add logSubjects stub + per-call capture), fixture SUBMODULE_STATUS new shape assertions, advanceTarget submodule ops called (fakeSpawn canned per-dir status), advanceTarget failure → warning not abort, caller behind/up-to-date/detached cases, verification ok/mismatch, advanced count/subjects cap (>15 via 20-subject canned log + aheadBehind count 20 → expect "... and 5 more"), abort-reason type usage `const r: (typeof SYNC_ABORT_REASONS)[number] = "divergent"`. sync-cli.test.ts: one full-shape assertion (caller/verification/submodules entry shape/count/subjects present), fakeClient += logSubjects.

Commit 2 — fix(prepare): detached-head guard + head from/to + post-rebase divergence (#6):
- src/prepare-recipe.ts: PrepareClient Pick += aheadBehind; detached guard before worktree guard (order: after branch resolution, before conflict check — or after; BEFORE any checkout/push per spec); reason "detached-head" added to PrepareAbort union; outcome += `head?: {from: string; to: string}` (revParse HEAD before ops when any of create/rebase/forcePush, after ops when rebase) + `aheadBehind?: {ahead: number; behind: number}` post-rebase vs base. renderGit unchanged. Update formatPrepare minimally (head line optional).
- tests/prepare-recipe.test.ts: fakeClient += aheadBehind stub + revs HEAD; new cases: detached (current:"" no explicit branch, create:true) → aborted detached-head, zero spawns, commands []; head from/to reported on rebase.

Commit 3 — feat(pr-finish): post-merge sync hint + runnable command recording (#7):
- src/pr-finish-cli.ts: recordingSpawn records `cmd === "git" && options?.cwd` as `git -C "<cwd>" args...` (quote like renderGit); post-mergeNow success warnings.push(`merge advanced origin's default branch — run sync_repo (full mode) in each active worktree / the default-branch worktree to catch up; this cwd may now be behind`). Grep confirmed nothing parses old form (only tests).
- tests/pr-finish-cli.test.ts: assert warning present on happy path; assert recorded commands for client.deleteLocalBranch etc. start with `git -C ` — NOTE client methods spawn WITHOUT -C (fake client doesn't spawn) — the recordingSpawn rewrite only affects spawns that carry options.cwd; the live client spawns (createBranchClient methods) mostly DON'T set cwd (spawn at default cwd repoRoot baked in createLiveSpawn(repoRoot))! CHECK createLiveSpawn in src/spawn.ts: if cwd defaults to repoRoot inside, options may be undefined for those calls → recorded form stays `git branch -D x`. Then #7b only rewrites when options?.cwd exists. Tests: use the runCi spawn probe style (echo probe with cwd) to assert `echo -C`?? echo isn't git. Craft: spawn("git", ["status"], {cwd:"/x"}) through a deps.spawn passthrough during runCi (like existing options-forwarding test) → assert recorded command === `git -C /x status`. Keep test simple along those lines.

Commit 4 — chore(tsconfig): widen typecheck to tests/scripts/extensions + fixes (#8-tsconfig, #8-code):
- src/sync-recipe.ts export `SYNC_ABORT_REASONS = [...] as const` (ACTUAL emitted set — see ground truth above; document divergence from spec list in report/PR) + `reason: (typeof SYNC_ABORT_REASONS)[number]` in SyncAbort; fix stale JSDoc.
- tsconfig.json: types += "@repo/pi-agent-ext-core-interface"; lib += "DOM"; drop rootDir/declaration/sourceMap/outDir; noEmit: true; include src/tests/scripts/extensions.
- package.json devDependencies += "@repo/pi-agent-ext-core-interface": "workspace:*" (then `bun install` from bun-apps/).
- Fix all probe errors (list in section above).
- scripts/deploy.ts `javascript-obfuscator`: prefer a tiny `scripts/types.d.ts` `declare module "javascript-obfuscator"` shim (used only under --obfuscate) — check if some other package already ships one to copy.

VERIFY: `( cd bun-apps/pi-agent-ext-devops && bun run check && bun test )`. Baseline test counts NOT yet captured — run `bun test 2>&1 | tail -3` on main state before edits for the report (tests were green pre-branch? unknown — capture first).

Then: commit, push, `gh pr create --title "devops: sync reporting, submodule semantics, prepare/pr-finish guards" --body <#1-#7 + tsconfig widening>` — DO NOT MERGE. Report: PR URL, changed files w/ one-liners, test counts before/after, deviations (spec's abort-reason literal list vs actual emissions; anything skipped).

## Watch-outs
- `.planning/` untouched; no CI workflow changes; no merge.
- Written artifacts in English; replies zh-TW.
- Don't top-level cd.
- entry.test.ts may parse extension source with regex (it errored on property access → objects) — after augmentation fix, re-run.
- If pi-agent cross-package imports (TS6059 fix by dropping rootDir) cascade errors from OTHER packages' files, consider `"exclude": ["node_modules", "dist", "scripts/lib/build-extensions.ts", "scripts/lib/codegen.ts"]`?? NO — those are needed (deploy.ts imports them). Accept checking pi-agent's two files; fix trivial errors there only if trivial, else note in report (they're already covered by pi-agent's own typecheck).
