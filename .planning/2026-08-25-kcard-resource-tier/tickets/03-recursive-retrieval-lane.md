---
type: task
blocking: 04
status: closed
resolution: implemented 2026-08-25 — heap lane live on USB4; see receipts
---

# 03 — Directory-recursive retrieval lane over resource rows

## Question
Does the upstream heap algorithm (global L0/L1 seed pass → per-directory scoped child search → parent→child score propagation → convergence) beat plain flat KNN over the same rows, enough to justify its lane?

## What to build
`resource-query` gains the recursive mode: embed the query once; global KNN over level∈{0,1} seeds a best-first max-heap of directories; per round pop ≤4 directories, run KNN scoped to their direct children (`WHERE parent = $uri`), combine `α·child + (1−α)·parent`, re-enqueue only directory rows, stop on ≤3 unchanged-top-k rounds or 3 stagnant rounds. Results carry uri + level + abstract preview + the descent trajectory (which path produced each hit). Tiered loading: `--tier 0|1|2` promotes lazily (L2 reads the file). The zettel `card` retrieval path and its default are untouched. α measured here (start 0.5, ablate 0.3/0.7).

## Acceptance
- [x] Fixture-tree tests: heap order, propagation arithmetic, convergence bound, trajectory recorded, L0/L1-only re-enqueue invariant — 19 hermetic tests in `__tests__/resource-recursive.test.ts` (fake client + `_testEmbedder`-shape deterministic embedder): seed ranking → expansion order pinned off the statement log; `final = α·rawSim + (1−α)·dirScore` asserted exactly at α∈{0.3,0.5,0.7} incl. two-hop chaining (0.6125 compound); converged (3 stable top-k rounds, 16/20 dirs — early stop), stagnant (pool frozen below limit), drained; trajectory `[seed sidecar, …sidecars, hit]`; re-enqueue invariant (L2 terminal, never a parent query on a file dirname); per-dir cap max(2·limit,20); mid-descent Surreal failure degrades per-dir
- [x] USB4 smoke (measured 2026-08-25 on this machine, production `context_db`): `resource-query "where is CLx power management defined" --mode recursive --top-k 8` → top hit `pages/page-536.md` (doc p.492 — the CL0s/CL1 power-saving-ratio field tables; 18 CLx mentions in the file), trajectory `pages/.overview.md → pages/page-536.md` inspectable in JSON + `via:` line; diagnostics seedCount 4, rounds 1, expandedDirs 2, stop drained, 159ms. `--root <vlm-out/usb4-…-clean>` + `--tier 2` lazily loads the L2 body (600-char preview) — verified live
- [x] α ablation recorded: 0.3 → top-3 sims 0.3349/0.3140/0.3132; 0.5 → 0.3891/0.3543/0.3530; 0.7 → 0.4433/0.3945/0.3927. **Finding: ranking is α-invariant on USB4** — the corpus is a SINGLE directory (839 children), so every hit shares one parent and the mix is monotonic in child sim; α compresses/expand scores only. α is unidentifiable from ranking until ticket 04's eval (or any multi-dir tree) — default stays 0.5 (THIS lane's ticket-measured default; upstream's own config default is 1.0 = child-only, `retrieval_config.py` — attribution corrected per reviewer N1), re-measure there
- [x] Lane is opt-in `--mode recursive|flat`, flat stays the default; the zettel `card` lane and the D36 default untouched (recursive module never references `card`/`index_meta`). NOTE: `--mode` is the shared CLI flag — `text|json|rpc` remain the OUTPUT modes; `recursive|flat` land in a separate `retrievalMode` field and never touch `out.mode`
- [x] Hermetic unit tests (no live Surreal, no network) + scratch-db integration: `resource-index-live.test.ts` gains the recursive round-trip (7-row fixture with hand-written sidecar pairs; skips when Surreal is down)
- [x] Canonical `bun run test` green: kcard 682/0 + tsc clean; s2-agent 977 (974 pass + 3 skip) + tsc clean; local_ci pass. Reviewer pass: independent reviewer subagent — NO blockers; 2 should-fix + 6 nits, all folded (see receipts)

## Implementation receipts
- `src/resource-recursive.ts` (new, kcard): seed pass is an EXACT scan over tier rows (`WHERE level IN [0,1]`, cosine in the SELECT) — tier rows are O(#dirs) so scan ≡ exact KNN without the v3.2.3 combined-predicate fallback ladder; child expansion computes cosine in the SELECT over the `resource_parent` index so vectors never cross the wire; upstream constants kept by name (MAX_PARALLEL_CHILD_SEARCHES 4, MAX_CONVERGENCE_ROUNDS 3, threshold >0 strict); the upstream literal-zero-parent raw-score quirk is kept but unreachable under the threshold (negative/zero dirs never seed) — documented + tested as such
- Determinism: every sort tie-breaks by tree/uri ascending; `DirHeap` is a score-desc sorted queue
- **Reviewer round (independent subagent, no blockers — all folded):** S1 `--tier` is a COLLECTION-time `maxLevel` filter in the lane (upstream's `level` param shape) + 5× over-fetch on the flat lane + a tier-aware empty message — a capped query no longer starves to 0 hits (re-verified live: `--tier 1` on USB4 returns the 4 tier rows with propagated scores). S2 seeds enqueue ONLY (upstream shape) — tier rows join the pool as descent children with mixed scores, never unmixed raw ones displacing file hits. N1 α attribution corrected (0.5 is this lane's default; upstream's is 1.0). N2 heap comparator returns 0 on full ties. N3 seed-pass Surreal failure → `semantic:false` (flat-lane contract) + test. N4 cap made observable: a sidecar ranked past the cap provably never expands its subtree (statement-log pin). N6 `tier2Body` containment check. N5 (`--mode` dual use) documented, no change. 22 hermetic tests (was 19)
- Drive-by (disclosed): `s2-agent/src/run-dir/registry-to-manifest.test.ts` literals gained `assets: []` — main's `tsc --noEmit` was RED since #2035 added the field to `RegistryDeployBlock` (pre-existing, verified against origin/main before fixing; blocked this ticket's typecheck gate)
