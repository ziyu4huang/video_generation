# t02 — bootstrap line + unit tests
superpowers.ts: env-derived exclusion names ∩ skill dirs → conditional
UNREADABLE line after the F2 paragraph; EXCLUDED_UNREADABLE_MARKER export.
bootstrap.test.ts: with-env contains marker + names; without-env absent;
env=default-only absent; ratchet cap holds.
