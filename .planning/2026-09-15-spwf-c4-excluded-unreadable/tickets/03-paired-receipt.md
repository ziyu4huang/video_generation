# t03 — paired C4 POST receipt
Deploy (new immutable dir) → sentinel grep → C4 leg (verbatim frozen
prompt, -ns + exclude env, pinned new dir) → forbid:brainstorming.
Pre-registered: PASS iff forbid ok; RED-with-sentinel = genuine finding.
