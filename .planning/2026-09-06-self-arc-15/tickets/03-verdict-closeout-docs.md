# t03 — verdict Decision, close-out docs PR, successor next-goal

Parent: `.planning/2026-09-06-self-arc-15/map.md` (D6, D8). Blocked on
t01–t02. Separate docs PR; merge pre-authorized (gh ship).

## Verdict Decision (pre-registered rule, map D6)

Apply to `results/comparison-v2.{json,md}` and write the map Decision with
NUMBERS:

- **Standing hypothesis**: role-split — bun-terminal owns production
  receipts (rendered truth), rpc is the structured complement.
- **Flip condition**: ≥1 complex dimension shows one lane MATERIALLY failing
  (FAIL confirmed by the re-run rule — flake ≠ signal) what the other passes
  STRUCTURALLY. Quantify: name the dimension, the cell(s), the ms/bytes/lines
  evidence, and the structural mechanism. Numeric score comparisons keep the
  >10% margin discipline (arc-13 D7).
- **Flip consequence**: a successor goal (role swap or migration), NEVER an
  in-arc rewrite (arc-13 D7 precedent).
- The Decision must literally answer: "which approach is better for the
  self-develop arc" — expected shape: per-dimension better-for, plus the
  overall role statement, each with a number or a structural citation.

## Work items

1. Map close-out: Decisions gains the verdict Decision (D-verdict, dated);
   tickets checked; Frontier drained or handed to the successor; Fog of war
   resolutions recorded; `status: done`; Shipped-as block (PR numbers, squash
   lineage, receipt paths, reviewer verdict per arc-13 shape).
2. Reciprocal cross-effort links: add the back-link on
   `.planning/2026-09-06-self-arc-13/map.md` and
   `.planning/2026-09-06-self-arc-14/map.md` (one line each: stress-tested by
   this arc, with the one-line outcome). Keep the disambiguation note vs
   `.planning/2026-09-09-self-arc-15/` (devops variant) intact.
3. Learnings: any NEW confirmed quirk (e.g. screen cumulative-latch ceiling,
  rpc error-field shape surprises) becomes a learnings-hardening-style entry
  or a hardening test — per the 2026-09-06-learnings-hardening discipline.
4. Successor next-goal: WRITE `output/next-goal-<ts>.md` BEFORE reporting
  done (hands-off rule, strict v2) — expected head: either the rpc
  complement's expanded wiring (if role-split holds) or the migration goal
  (if flipped), derived from the verdict numbers.
5. Docs PR → local gates → `gh ship` (squash). Confirm merged; sweep the
   branch.

## Done when (arc Definition of done)

Complex suite merged (implementation PR squash-merged); both lanes' complex
receipts executed green-of-infra on the deployed tree (every cell verdicted,
no driver/adapter failure); `results/comparison-v2.{json,md}` committed; the
map carries the verdict Decision answering "which approach is better for the
self-develop arc" with numbers; successor next-goal validated.
