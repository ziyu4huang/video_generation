## Question
Get `gating:{ core: true }` onto the 4 pi-coding-agent built-ins (`read`, `write`, `edit`, `bash`) via the mechanism ticket 01's research establishes (sibling-repo edit / in-repo augmentation hook / cross-repo PR). Once all 4 are owner-declared, they're removed from `CORE_TOOLS` fallback eligibility (`buildEffectiveGates` marks owner-declared names `handled`). Reshapes based on 01's findings — if 01 proves the built-ins can't be migrated from here, this ticket either becomes a cross-repo PR task or hands a residual built-in set to ticket 04.

type: task
blocked by: 01
status: open
