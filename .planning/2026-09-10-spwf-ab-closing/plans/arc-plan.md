Plan is ready. All context bullets below were verified by me this session against the actual bytes (read budget: ~15 files).

---

# Plan: `2026-09-10-spwf-ab-closing`

## Destination

Close the A/B arc's residuals with evidence, not narrative: C2-glm gets a settled compliance verdict under a same-turn-batch-aware detector (built, validated against preserved bytes, promoted to a committed home with a regression test), C1 salience gets a 5-sample YES-rate with pre-committed branch actions, the gemma column gets one honest quiet-window attempt (or a dated defer), and the F3 repo-gate question gets an evidence-based intended-vs-defect ruling that re-sets the C5 expectation. End state = the closing map's verdict matrix has no detector-artifact PARTIALs, every flipped verdict cites committed evidence under `.planning/2026-09-10-spwf-ab-closing/evidence/`, the harness lives in `bun-apps/s2-agent-ext-wayfind/scripts/` behind a unit test, and the loop ritual closes (reviewer GLM-5.3, status flip in the landing PR, validated successor next-goal).

## Context (planner-verified, cite file:line)

- **The C2 PARTIAL is (very likely) a double detector artifact — this reshapes ticket 1.** I decoded the preserved postfix session (`output/spwf-ab/postfix/glm-C2.json` → `sessionFile`): line 5 = `assistant thinking,text,TOOL(read),TOOL(bash)` where bash is `ls -la … && cat … || echo NO_FILE` (pure recon), lines 6–7 = toolResults, **line 8 = `TOOL(write)` of hello.ts**. The read turn (msg 5) strictly precedes the write turn (msg 8) — the model could only have issued the write after the skill text returned. Two bugs conspired: (a) line-based `idx` collapses toolCalls sharing an assistant message (`drive-case.ts` `detect()`, ~line 170: `idx += 1` per JSONL line); (b) the mutation regex counts ANY bash touching `output/spwf-ab` as mutating (`drive-case.ts:184`: `/^(write|edit|multiedit|bash)$/.test(name) && /output\/spwf-drive|…/.test(a)`), so the read-only `ls|cat` was the "mutation" at idx 5. Under correct semantics the leg-2 bytes are already COMPLIANT; leg 3 still runs fresh (below).
- **Dist pin verified**: only `/Users/huangziyu/proj/dist/s2-agent-sh/darwin-arm64/0.10.3+gc172fd3/ext/superpowers/ext.cjs` greps the F2 directive line ("check the available skills list") = 1 hit; `current` → same dir; older dirs (g8840611, g88611db, g9feaa18) = 0. Legs pin `0.10.3+gc172fd3` by dir path, never `current`.
- **F2 line in source**: `bun-apps/s2-agent-ext-superpowers/src/superpowers.ts:283` (getBootstrapContent, the "Before you act on any task…" paragraph).
- **C5-glm read path**: the session shows exactly one skill read — `.agents/skills/using-s2-agent-skills/SKILL.md` (the repo gate), reads=`[using-s2-agent-skills]`, no leaf skill. The gate's own text (`.agents/skills/using-s2-agent-skills/SKILL.md`, "Route-first gates" intro): *"the gates below ARE the trigger layer — CLAUDE.md points here. On ANY of these situations, READ + follow the named ext SKILL.md first"* — the Tickets/effort gate routes "via ask-matt when unsure; to-spec then to-tickets". The gate is explicitly designed as the model-side entry; C5's answer was correct from its routing table.
- **Promotion surface exists**: `bun-apps/s2-agent-ext-wayfind/scripts/` already holds runnable entries (effort-audit.ts, probe-ext.ts, sweep-zero-citation.ts); the allowlist contract is `bun-apps/s2-agent-ext-devops/tests/scripts-dir-contract.test.ts` (snapshot allowlist, precedent line form at :36; test at :120). `bun-apps/s2-agent-ext-wayfind/tests/` exists (arc-ledger.test.ts).
- **Evidence caps fit**: postfix receipt 4KB, its session JSONL 30KB — both under the 256KB/file, 1MB/effort caps (`.planning/CONVENTIONS.md` § Evidence permanence tier, adopted 2026-09-10 — AFTER the A/B arc opened, so its output/-only citations were convention-legal then; this arc must do better).
- **Naming**: content slug per CONVENTIONS § Finished-means-terminal-with-provenance ("do not name new efforts after self-arc-N"); the dispatch pins `2026-09-10-spwf-ab-closing`. Keep that name verbatim even if execution slips a day.
- **Collision**: sibling audit-gate-fidelity touches devops local_ci src + ~19 ext `package.json`. This arc touches neither (promotion = new files under wayfind + ONE allowlist line in a devops *tests* file). Rebase churn possible; no file-level conflict.

## Decisions

- **D1 — In scope**: queued items 1 (C2 detector + leg 3), 3 (C1 probe), 2 (gemma, precondition-gated), 5+7 merged (F3 ruling subsumes the C5 expectation design), 6 (harness promotion, last, gated on the detector landing). **Deferred with reason**: item 4 (session_compact) — needs a compact-triggering long session, high token cost, zero adjudication pressure on any open verdict; it stays in the successor's ranked list.
- **D2 — Detector semantics (frozen before any run, per D5 discipline carried over)**: every toolCall gets `(msgLine, callOrdinal)`; C2 order check = `order:read-turn-before-mutate-turn`: COMPLIANT ⟺ skill-read exists ∧ (no mutation ∨ `firstRead.msgLine < firstMutate.msgLine`); same assistant message = NON-COMPLIANT ("same-turn batch"). Bash is "mutating" ONLY on an explicit write-operator list (`>`, `>>`, `tee`, `sed -i`, `rm`, `mv`, `cp`, `mkdir`, `touch`, `patch`, `ln -s`, heredoc-to-file, `git commit|checkout|switch|rebase|merge|stash|restore`, `bun add|remove|install`, `npm i|install|remove`, `chmod|chown`, `dd`, `*pip install`); recon (`ls`, `cat`, `rg`, `head`, `stat`, `test`, `echo` sans redirect) is non-mutating; unknown → non-mutating BUT the full command is recorded in `detected.bashCalls[]` for human audit. C3 test-before-impl compares `(msgLine, callOrdinal)` lexicographically.
- **D3 — Settled C2 verdict rule**: leg-2 bytes re-adjudicated by the new detector AND fresh leg 3 both COMPLIANT → CLOSED-PASS; leg 3 shows a true same-turn batch → verdict NON-COMPLIANT recorded as finding (consultation achieved, compliance model-dependent) — NO prompt re-rolls (prior D6 carried verbatim).
- **D4 — C1 adjudication branches (pre-committed)**: ≥4/5 YES → salient-enough, F1 recorded as self-report noise, no fix; 3/5 → marginal, no fix, note the instrument's limited resolution; ≤2/5 → branch on behavioral evidence: if C2-leg-3 and battery reads show the bootstrap's behavioral line firing, the self-report is the unreliable instrument → retire C1 self-report as a salience detector in future matrices (map decision); if behavioral misses accompany it → ONE minimal strengthening line in `getBootstrapContent` (F2 precedent), paired receipt, PR. Never more than one strengthening.
- **D5 — gemma quiet-window precondition**: 3 consecutive `/v1/models` polls ≥60s apart, each showing `google/gemma-4-12b` resident and ≤1 large chat model total; then C2/C3/C5/C8 (C4-gemma = stretch), ≤2 legs/cell, `--skip-on-contention`, cap 300s. Max 2 windows across the arc; else defer with a dated fog note (blocker is sibling-owned — escalation is a note, not a burn).
- **D6 — Harness promotion in, package.json out**: promote the upgraded `drive-case.ts` + battery to `bun-apps/s2-agent-ext-wayfind/scripts/` with one allowlist line; NO `package.json` edits anywhere (sibling collision YELLOW). The regression test needs a committed home — that is the argument for doing promotion this arc rather than deferring.
- **D7 — Evidence**: every verdict the closing map flips cites `.planning/2026-09-10-spwf-ab-closing/evidence/` (receipt + the load-bearing session JSONLs), never `output/`.
- **D8 — F3 ruling is evidence-bound**: if the gate-first route is upheld as intended (my read of the gate's own text says it will be), the C5 expectation becomes read-any {`using-s2-agent-skills`, `to-spec`, `to-tickets`, `ask-matt`} and the EXISTING C5-glm receipt is re-adjudicated — no new glm leg. If evidence says defect → gh issue via issue-tracker, fix deferred to successor.

## Tickets

- **t01 — effort open + pin re-verify**. Sync via devops chain; confirm zero open PRs; create `.planning/2026-09-10-spwf-ab-closing/{map.md,spec.md? no — map + tickets suffice}`; commit early per standing rule. Re-verify the pin by artifact not label (grep `0.10.3+gc172fd3/ext/superpowers/ext.cjs` for the F2 line; confirm `current`'s target). One cheap smoke leg (trivial "reply ok" prompt, cap 120, pin zai/glm-5.3, `--dist 0.10.3+gc172fd3`) asserting `pin:match`. *Acceptance*: map + tickets committed; smoke receipt PASS with pin:match.
- **t02 — same-turn-batch-aware detector (scratch) + preserved-bytes validation**. Rework `output/spwf-drive/drive-case.ts` `detect()` per D2; split the pure part into an importable `detectFromLines(lines)` (t07 needs it). *Acceptance (dry-runs, zero new tokens)*: (i) postfix C2 session → order PASS (read msg5 / write msg8), full verdict PASS; (ii) pre-fix `output/spwf-ab/glm/C2.json` session → still RED (0 reads); (iii) `glm/C3.json` session → still RED (honest impl-first must survive the upgrade — regression guard); (iv) synthetic read+write-in-one-message JSONL → NON-COMPLIANT.
- **t03 — C2 settle (leg 3 of 3)**. Fresh C2-glm: frozen C2n prompt verbatim (`.planning/2026-09-10-spwf-drive-ab/tickets/03-ab-battery.md`), wipe `output/spwf-ab/scratch/hello.ts`, pin gc172fd3 + zai/glm-5.3, cap 300, out `output/spwf-ab-closing/c2-leg3.json`. Apply D3. Commit to `evidence/`: leg-2 receipt (4KB) + leg-2 session JSONL (30KB) + leg-3 receipt + leg-3 session. *Acceptance*: settled verdict written in-map with both legs cited; evidence sizes under caps.
- **t04 — C1 salience 5-probe**. 5 legs, C1 prompt verbatim, distinct nonces, pin zai/glm-5.3, cap 300, receipts under `output/spwf-ab-closing/c1-probe/`. Record YES-rate; adjudicate per D4 in the map. *Acceptance*: 5 receipts each with pin:match; branch action executed exactly as pre-committed.
- **t05 — gemma quiet-window column (precondition-gated)**. Poll protocol per D5; run C2/C3/C5/C8 (+C4 stretch) when quiet; every poll recorded. *Acceptance*: cells filled with PASS/RED/SKIP receipts, OR a dated defer note naming the blocker (sibling LM Studio churn) — both are terminal.
- **t06 — F3 repo-gate adjudication + C5 re-set (read-only, zero tokens)**. Weigh: gate SKILL.md's trigger-layer design, the C5-glm session bytes (gate read, answer correct), s2-agent's registry description auto-fire (`bun-apps/s2-agent/src/registry-config.ts`, `skills: true`). Rule per D8; if upheld: re-set the C5 expectation, re-adjudicate the existing receipt, add ONE clarifying line (gate-first is a legitimate model-side route) to the gate skill or the wayfind skill — whichever owns routing docs. *Acceptance*: ruling recorded as a Decision with file:line evidence; C5 matrix cell re-verdicted without a new leg (or issue filed).
- **t07 — harness promotion + regression test (needs t02)**. Move upgraded `drive-case.ts` → `bun-apps/s2-agent-ext-wayfind/scripts/drive-case.ts`, `battery.sh` → `scripts/spwf-battery.sh`; fix `REPO` resolution depth (`import.meta.dir/../../..`); add the allowlist line to `scripts-dir-contract.test.ts`; add `bun-apps/s2-agent-ext-wayfind/tests/drive-case-detector.test.ts` with the 4 synthetic fixtures from t02 acceptance. Run wayfind + devops package gates (`bun run check`/`typecheck`/`test` per package discipline). *Acceptance*: tests green locally; allowlist line present; zero package.json edits.
- **t08 — close-out**. Matrix deltas recorded in the closing map (C2 settled, C5 re-verdicted, gemma filled/deferred, C1 rate); add `Completed-by: 2026-09-10-spwf-ab-closing` to the A/B map's Cross-effort links (same PR); `effort-audit.ts` exit 0; independent reviewer GLM-5.3 (receipt under `output/reviewer-spwf-ab-closing/`); devops PR chain; **status flip in the landing PR**; successor next-goal (strict v2, validated, `LATEST-next-goal.md` re-pointed) BEFORE reporting done — session_compact and any C1/bootstrap strengthening carry there.

## Frontier / fog

- **Frontier: t02** — every verdict flip and the promotion test hang off the detector; it is pure post-hoc work over preserved bytes (zero tokens), so it goes first.
- Fog: zai stall recurrence (→ SKIP, receipt preserved, never re-rolled past cap); LM Studio churn windows (t05 gated, defer fallback); `current` moving (pinned dirs immune); sibling PR churn on devops/package.json (no file overlap, rebase only); C1 probe outcome ≤2/5 opens a behavioral-evidence branch that depends on t03 having landed — run t03 before adjudicating t04.

## Immediate executor sequence

1. `bun bun-apps/s2-agent-ext-devops/src/sync-default-branch-cli.ts` (or wrapper) → verify tip = `61688652`, zero open PRs.
2. t01: open `.planning/2026-09-10-spwf-ab-closing/` (map + tickets t01–t08), commit on a feature branch via devops chain; grep-verify the gc172fd3 pin; smoke leg.
3. t02 detector upgrade + the four preserved-bytes/synthetic validations (no tokens).
4. One zai sitting for t03→t04: C2 leg 3, then the 5 C1 probe legs; adjudicate D3/D4 in that order.
5. t05 opportunistically whenever the quiet-window precondition holds; t06 (read-only) any time.
6. t07 promotion + regression test; package gates; PR.
7. t08 close-out: reviewer, matrix, status flip, `Completed-by` link on the A/B map, validated successor next-goal — then hands off.

**Which operating learning applied**: the mirror of Learning 2 — *the label is not the content*. "PARTIAL (batched same-turn)" was a verdict label; reading the actual artifact (the session JSONL bytes behind the receipt) showed the detector, not the model, manufactured the collision — a read-only recon `bash` sharing a line with the skill read, while the real write came a full turn later. Verify the artifact, then trust the label; never the reverse.