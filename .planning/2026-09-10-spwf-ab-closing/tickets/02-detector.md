# t02 — same-turn-batch-aware detector (scratch)
drive-case.ts detect() per D2: (msgLine, callOrdinal) per toolCall; bash
write-operator list; bashCalls[] audit record; C3 lexicographic order.
Validation dry-runs (zero tokens): (i) postfix C2 session → order PASS +
verdict PASS; (ii) pre-fix glm/C2 session → still RED (0 reads);
(iii) glm/C3 session → still RED (impl-first survives); (iv) synthetic
read+write-in-one-message → NON-COMPLIANT.
