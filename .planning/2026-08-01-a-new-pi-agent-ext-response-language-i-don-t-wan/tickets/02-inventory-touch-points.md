## Question

What is the complete, enumerated set of files/registrations the migration must touch? (Foundational research — clears fog for the branch-strategy and execute tickets.)

type: research
status: closed

## Resolution

**Move (source + tests):**
- `pi-agent-ext-response-language/src/command.ts` → `pi-agent-ext-core-task/src/response-language/command.ts`
- `pi-agent-ext-response-language/src/settings.ts` → `pi-agent-ext-core-task/src/response-language/settings.ts`
- `pi-agent-ext-response-language/tests/{command,settings,entry}.test.ts` → `pi-agent-ext-core-task/src/response-language/__tests__/` (fix import paths; core-task colocates `__tests__` under each feature dir)

**Add (registration):**
- New `pi-agent-ext-core-task/src/response-language/response-language.ts` exporting `registerResponseLanguage(pi)` (mirrors the `ask-user` / `todo` `registerX(pi)` pattern).
- Call `registerResponseLanguage(pi)` in `extensions/core-task.ts`.

**Delete (the standalone package):**
- Entire dir `bun-apps/pi-agent-ext-response-language/` (package.json, tsconfig, README, `extensions/`, `src/`, `tests/`, `node_modules` link).
- manifest block in `bun-apps/pi-agent/run-dir/manifest.json` — the `pi-agent-ext-response-language` `name` / `entry` / `testGate` (≈ lines 61-64). core-task is already registered (≈ line 84), so no new entry is added.
- `bun-apps/bun.lock` workspace entries for `pi-agent-ext-response-language` (≈ 3 spots) — regenerated by `bun install` run from `bun-apps/`.

**No edit needed (auto-handled):**
- `pi-agent-cli/src/commands/schema-cost.ts` `EXTRA_ENTRIES` is `[]` — the canary derives its list from `manifest.json`, so removing the manifest entry auto-handles schema-cost.
- `CLAUDE.md` (line 9) references the `/response-language` command *by usage*, not by package; the command name is unchanged, so the doc stays correct.

**Stays put (do NOT move):**
- `pi-agent/src/patches/force-response-language.ts` (+ `.test.ts`, and its wiring in `index.ts` / `index.test.ts`) — the patch half; out of scope.

**Deps:** core-task already has the SDK peer dep; the command needs only `getAgentDir` (SDK). No new dependency.

**No divergent duplicate (corrected):** the introducing commit `4fbc35b2` is an ancestor of `origin/main`, and `git diff origin/main HEAD -- <package>` is empty — the package is byte-identical on both lines via one shared commit. No conflict is possible on it; the deletion applies cleanly on any base. See [branch-divergence-strategy](04-branch-divergence-strategy.md).
