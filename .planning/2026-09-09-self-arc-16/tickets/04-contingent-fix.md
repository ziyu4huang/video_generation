# Ticket 04 — CONTINGENT fix: what the matrix uncovers (T4)

Status: open · interleaves wherever a red receipt lands

## Work
Fix named by the first red receipt. Protocol: skills-parity drift → grep the
deployed ext.cjs / skills bytes FIRST (learning #1), redeploy if stale;
frontmatter drift → fix the skill; deployed wayfind_effort execute failure →
root-cause deployed bytes before blaming source. Regression test wherever a
PRODUCT bug (not a driver bug) is found.

## Done when
Every red check green on a re-run receipt — OR closes NO-OP citing the PASS
receipts (evidence, not assertion).
