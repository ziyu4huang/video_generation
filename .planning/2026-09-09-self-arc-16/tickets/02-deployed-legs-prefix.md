# Ticket 02 — deployed-leg receipts PRE-fix (T2)

Status: open · after t01 · BEFORE t03 (pre-fix contract evidence)

## Work
Plain-bun scratch drivers (absolute-path imports): resolve `current` → record
version label; per family: `ext-dir-present`, `ext.json` parses, sha256 skills
parity vs source; wayfind: `loadExt` ok + `tool("wayfind_effort")` + READ-ONLY
execute (list-style, temp `.planning` fixture, arc-13 D4 stub-ctx shape);
superpowers: `loadExt-expected-throw` PASSES by confirming the documented
limitation (message captured as evidence); `commands-surface` = RECORDED-GAP.

## Done when
`output/self-arc16-wayfind-deployed-<date>/receipt.json` and
`output/self-arc16-superpowers-deployed-<date>/receipt.json` PASS with every
check named; gaps recorded as gap entries with reasons. Failing receipts are
kept (fix forward, never delete).
