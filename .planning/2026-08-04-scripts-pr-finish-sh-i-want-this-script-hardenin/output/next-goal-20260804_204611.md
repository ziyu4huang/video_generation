# Next goal: build the `land-pr` agentic skill

> Closing-ceremony handoff from effort
> `2026-08-04-scripts-pr-finish-sh-i-want-this-script-hardenin` (map complete — 6/6
> decisions, 0 open tickets). The planning is done; this is the *do-the-thing* goal it
> hands off to.

## Goal (concrete, non-gated, non-conflicting)

Build `pi-agent-ext-devops/skills/land-pr/SKILL.md` per the completed map, then prove +
retire the script:

1. **Skill body** — orchestrate the existing tools (`await_pr_merge` / `pr_status` /
   `sweep_branches`) + plain git, with two intelligence sections the tools lack:
   - the **flake-judgment rubric** (#03): map failing check → package (from `name`) →
     intersect with `gh pr diff --name-only`; untouched → `gh run rerun --failed` once +
     re-await; touched / monorepo / unmappable + any 2nd failure → escape-hatch (name +
     `state` + duration + diff-relevance verdict + ready command, then stop).
   - the **worktree-aware post-merge cleanup** (#04, draft in `drafts/post-merge-cleanup.md`).
2. **Wire discovery** (#01) — add `"files": ["skills/", …]` + `"skills": ["./skills"]` to
   `pi-agent-ext-devops/package.json`. Verify `/skill:land-pr` loads (see deferred prize).
3. **Targeted unit tests** (#06) — the rubric's decision fn (name+`state`+diff →
   rerun/surface, incl. monorepo/unmappable + shared-code-guard) + the worktree-cleanup
   command sequence. Fixture raw `gh pr checks` JSON (#05 — no typed-tool surface needed).
4. **Dogfood** (#06) — land ~3 real PRs end-to-end without falling back to `pr-finish.sh`,
   incl. ≥1 flake-rerun. **PR #1023 (docs-only, `test · pi-agent-ext-movie-director`
   timed out) is the canonical flake-rerun candidate** — rerun it as the first dogfood.
5. **Retire** — delete `scripts/pr-finish.sh` once the bar clears. (Companion scripts
   stay — #02 confirmed no code callers.)

## Deferred prizes (harvested from this map's Not-yet-specified)

- **Broader devops skill set** — if a second devops skill (release / rollback) arrives,
  revisit #01's session-start-bootstrap decision (the bare-package mechanism was chosen
  for an *on-demand* skill; an *orientation* skill would warrant the
  `resources_discover` + session-start bootstrap).
- **Empirical bare-package-skills-load check** — confirm `/skill:land-pr` actually loads
  from an *extension* package (vs needing explicit `resources_discover` registration like
  superpowers). Verify at first build; if it doesn't load, add a minimal `resources_discover`
  in `extensions/devops.ts` (still no session-start bootstrap).

## False premises / footguns (reflective — only the charting session knows)

- **Premise I nearly shipped on**: "port `pr-finish.sh` into devops." Wrong — the
  extension ALREADY had the tool mechanics (`await_pr_merge` / `sweep_branches`, 91
  tests, structured JSON). The real prize was the **judgment layer on top**, not a port.
  Reading the extension's README + `extensions/devops.ts` *first* corrected this.
- **`gh pr checks --json` has NO `conclusion` field** — `state` is the
  conclusion-equivalent (`FAILURE` / `TIMED_OUT` / `CANCELLED` / `STARTUP_FAILURE` …).
  The rubric's wording shifted from "conclusion" to `state`. When writing the skill,
  don't reach for a `conclusion` field.
- **Effort slug is verbose** (`…-i-want-this-script-hardenin`, auto-generated from the
  prompt). A concise slug like `2026-08-04-devops-land-pr-skill` would read better; not
  worth renaming mid-chart, but prefer concise slugs at `/wayfind` time.
- **Wayfinder ticket edits are byte-fragile** — em-dash / curly-quote / line-wrap
  mismatches aborted two `edit` calls. Re-read the file before editing exact-text hunks.
