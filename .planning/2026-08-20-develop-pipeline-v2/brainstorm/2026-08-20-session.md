# 2026-08-20 session: Pipeline v2 design dialogue

## Tier system (spec §1)
Mechanical size rules routing: T1 (≤3 files, no interface/schema) → bounded chat → execute-t1; T2 (4-10 files or 2 packages) → quick grill → thin spec → execute-plan; T3 (≥3 packages or runtime core) → full spine → superpowers plan → execute-plan fan-out. Declaration via map.md frontmatter (T2/T3) or --tier commit trailer (T1). Anti-drift via pipeline-gate size verification.

## Engine division (spec §2)
Workflow promoted to primary execution engine; superpowers executing-plans becomes driver/judgment (read plan, handle red, decide redispatch vs systematic-debugging). Standard templates: execute-plan (T2/T3 fan-out: Gate→Execute→Janitor→Report), execute-t1 (single task), review-plan (unchanged). Evidence base from 2026-08-16 carries into templates (150-260k tokenBudget, 6-14 maxTurns, mandatory verify child + janitor).

## Record schema (spec §3)
Unified dispatch record: {effort, tier, ticket, engine, tokenBudget, maxTurns, outcome, SHA, gate, ts}. Query via dispatch-log CLI; learning loop via devops_retrospect → hermes-memory; T1 records land despite no effort folder.

## Gate checks (spec §4)
pipeline-gate mechanical enforcement: tier-size match (all), frozen map (T2/T3), settled spec (T2/T3), Run:/Expected: per task (T2/T3), dispatch ledger complete (T2/T3). Two call sites: workflow Gate phase, standalone CLI. No LLM/network; testable.

## Migration order (spec §5)
Gate first (Task 1), records (Task 2), templates (3-4), superpowers rewrite (5), docs (6). This effort dogsits the T3 path: wayfind → superpowers plan → workflow execute → gate. Old two-runtimes D1 superseded by D5 revision (handoffs need teeth).
