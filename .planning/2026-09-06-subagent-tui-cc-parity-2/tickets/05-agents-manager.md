# 05 — /agents definition-management dialog (G5) — OPEN, own effort

CC's interactive /agents surface (list/create/edit/delete agent definitions,
tools/model/permissions per agent). Intentionally NOT charted in the prior
wave (big surface). Needs confirm-gates (key claims? presets.ts vs
builtin-agents shape? webui parity?) — its own effort, not this session.
