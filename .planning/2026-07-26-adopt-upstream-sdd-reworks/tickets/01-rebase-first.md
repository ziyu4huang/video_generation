## Question

Rebase this branch onto `origin/main` (13 commits behind at chart time) before executing the SDD re-pin, so the fork↔upstream diff and the re-pin land on current ground.

**type:** task (AFK)
**claimed:** _(open)_
**blocked by:** —

## Resolution

_(open — the user's call; state-changing. Run `git fetch && git rebase origin/main`, resolve, then proceed. The research in 02/04 was gathered against the stale tree and should be spot-rechecked after rebase.)_
