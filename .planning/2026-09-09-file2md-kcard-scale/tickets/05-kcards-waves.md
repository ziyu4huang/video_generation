# Ticket 05 — kcards waves + convergence

Status: done

## Goal

10 kcards (one per corpus paper, house format), every numeric bullet naming
its Table/Figure + page anchor (D5), every vision bullet provenance-labeled
and ground-checked via `src/vlm/ground.ts` (D5), converged into the vault
graph via the bridge with explicit vault + on-disk verification (D7).

## Done when

- [x] 10 cards in `Zettelkasten/Paper - *.md`, house format
- [x] grounding receipt: per-paper ground results, all admitted bullets clean
- [x] convergence artifacts verified on disk (graph notes + semantic index + MOC)
