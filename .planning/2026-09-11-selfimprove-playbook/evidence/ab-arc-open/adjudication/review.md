# Adjudication — spwf-ab gemma fill: WITH vs WITHOUT playbook

**Grounding (what I re-measured, read-only):** the decisive factual fork between the plans reproduces on this machine right now: `curl 127.0.0.1:1234/v1/models` → **7 ids** (bonsai-27b, gemma-4-12b-qat, qwen3.8-27b, gemma-4-12b + 3 embedders) while `lms ps` → **only `text-embedding-bge-m3` loaded (634.55 MB, IDLE)**. The inherited oracle — `drive-case.ts:303-317` (`contentionPrecheck` counts ≥7B non-embedding ids from `/v1/models`) — therefore reports 4 large **at all times on this box**. Also verified: `spwf-battery.sh:8` hard-codes `cd /Users/huangziyu/proj/video_generation__superpowers`; pin `0.10.3+gc172fd3` exists, `grep -c "check the available skills list" ext.cjs` = **1**, `current` → `g6423400`; memory worktree is on `selfimprove-playbook` with the exact 4 dirty files both plans report; matrix shows gemma C1 = **YES (round 2)**, C2–C8 gemma = UNTESTED-infrastructure; next-goal:53,67 confirm the `output/spwf-ab-closing/gemma-fill/` path and the "second dated note" escalation branch. Both runs used the same model (glm-5.3) and 11 turns (plan-receipt.json), so the A/B is controlled.

## 1. Which plan executes the fill more safely?

**Split: WITHOUT wins the make-or-break axis (precondition), WITH wins operational hygiene — and the precondition flaw is blocker-grade.**

- **Precondition protocol — WITHOUT, decisively.** WITHOUT measured both oracles and found `/v1/models` is a catalog, not residency (`without/plan.md:14`), amending D5 to `lms ps` residency with churn-stability + gemma-in-catalog, recording both raw oracle outputs per poll (`:22-23`) and the amendment as a map decision (`:20`). WITH keys its poll to "drive-case.ts's own predicate (same ruler as the harness precheck — PB-12)" (`with/plan.md:26`) with "Green ⟺ … count ≤ 1" (`:31`) — an oracle that **can never go green here** (4 large permanently in catalog, verified above). WITH's plan deterministically burns 2 windows → defer → its own Destination ("matrix stops being UNTESTED-infrastructure") is unreachable by construction. **Blocker (WITH).**
- **Pin discipline — tie.** Both keep `gc172fd3`, byte-verify the F2 literal, never `current`, same single-variable A/B rationale (`with/plan.md:35`; `without/plan.md:38`); I re-ran the grep (=1) and the symlink (`g6423400`) — both correct.
- **Receipts — near tie.** Both: `.planning` evidence, model-attribution voids, SKIP preserved, ≤2 legs/cell, frozen prompts. WITHOUT additionally reconciles the next-goal's `output/` path against D7 explicitly (`without/plan.md:40`); WITH never cites the next-goal at all.
- **Worktree/leg-runner safety — WITH.** WITH forces a dedicated worktree (PB-01/PB-02, `with/plan.md:10,18`) and defuses the `spwf-battery.sh:8` cd-hazard by invoking `drive-case.ts` directly (`:43` — hazard verified). WITHOUT's "sync to main first" on a dirty sibling-occupied tree is underspecified (`without/plan.md:11`), and its **primary** leg path edits + runs the battery (`:37`), which cds back into the occupied memory worktree — the exact foot-gun WITH caught. **Should-fix (WITHOUT).** WITHOUT's optional detector-refinement ride in t02 (`:30`) also mildly muddies cross-column receipt comparability — should-fix scope creep, though the precheck fix itself is arguably necessary to unblock D5.

## 2. Does WITH demonstrably apply playbook strategies WITHOUT lacks?

**Yes — but only on open/close hygiene, not on measurement.** WITH-only applications: **PB-01/PB-02** (dedicated worktree from origin/main tip vs WITHOUT's bare "sync to main first", `with/plan.md:10,18` vs `without/plan.md:11`); **PB-05+PB-20** (status flip + Shipped-as in the same PR, `effort-audit.ts` exit-0 gate, `with/plan.md:39` — WITHOUT's t05 has reviewer/PR/status-flip but neither); **PB-04** (validated successor → LATEST repoint → doctor chain, `with/plan.md:39`); **PB-16** (bidirectional map cross-links, `with/plan.md:38` — absent in WITHOUT's t04); **PB-03** (push-all before successor, explicit only in WITH). Conversely, the receipt/pin core (PB-08, 09, 11, 12-attribution, 13, 14, 18) appears in **both** — WITHOUT inherits it from the drive-ab/closing maps' D-decisions and CONVENTIONS, so it is common ancestry, not playbook marginal value. The sharpest datum: WITH cites **PB-12** to justify *reusing* the ruler (`with/plan.md:26`) while missing PB-12's own second clause ("suspect the ruler before the subject", playbook PB-12) — the exact antidote to its blocker; WITHOUT, with no playbook, applied ruler-suspicion anyway via learnings #2 (`without/plan.md:14`). WITH also re-runs C1 (`with/plan.md:54`) though the matrix already holds gemma C1 = YES after 2 rounds — a third leg breaching the ≤2-legs/cell budget WITH itself cites (PB-14). **Should-fix (WITH)**; WITHOUT correctly says "Skip C1 (already YES)" (`without/plan.md:38`).

## 3. Verdict

**PLAYBOOK-DISADVANTAGE**

Rationale: the playbook's demonstrable marginal effect on WITH was close-out/open ceremony (PB-01/02/03/04/05/16/20 — real but secondary) plus a citation-flavored justification for reusing an unverified ruler, yielding a plan whose precondition can never pass on this machine and that re-runs an already-filled cell against its own budget. The no-playbook planner spent the same verification budget on the measurement itself, found the catalog-vs-residency flaw that decides whether the arc can succeed at all, picked the correct cell set, and matched WITH on every pin/receipt discipline — so on this directive the playbook prep correlated with worse epistemics, not better.

**Findings roll-up:** WITH — 1 blocker (unsatisfiable `/v1/models` precondition, `with/plan.md:26,31`), 1 should-fix (C1 re-run vs PB-14 budget, `:54`). WITHOUT — 2 should-fixes (no dedicated worktree / "sync to main first" on dirty sibling tree, `without/plan.md:11`; battery primary-path cd-hazard + optional scope ride, `:30,37`). Nothing else to fix; pin discipline, evidence discipline, and defer semantics are correct in both.