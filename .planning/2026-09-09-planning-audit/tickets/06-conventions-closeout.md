# t06 — conventions + close-out

- Two bullets in `.planning/CONVENTIONS.md`: (1) close-out PRs MUST flip the
  effort's frontmatter status in the same PR that lands Shipped-as; (2) new
  effort dirs use content slugs — self-arc-N belongs to the closed series,
  claimed at merge time by parallel sessions.
- Map → done + Shipped-as; independent reviewer pass on the whole diff.
- Successor next-goal validated, superseding
  `output/next-goal-20260909-051936.md` per the strict-v2 chain (default
  candidate: live-agent closure of the 3 RECORDED-GAP items + routing
  end-to-end receipt).

Acceptance: close-out PR merged through the devops chain; successor file
exists and validates.
