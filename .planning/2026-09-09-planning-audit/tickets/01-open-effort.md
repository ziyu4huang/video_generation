# t01 — open the effort

Write `map.md` (house shape) + `tickets/01–06` + `plans/arc-plan.md` (the
planner's verbatim plan, already copied). Commit on `feat/planning-audit`.

Acceptance: dir committed on the feature branch; `git status --porcelain --
.planning/` clean.
