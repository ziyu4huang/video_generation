# Next goal — devops effort closed (2026-08-07-yes-look-at-it-...)

## Destination reached
Improved pi-agent-ext-devops gh/local-CI tooling: local_ci tool + await_pr_merge gates on it (local-CI-first, remote CI disabled) + honest sweep_branches. Tickets 01-06 closed; #1045/#1048/#1050/#1054/#1055 on main.

## Deferred prizes (harvested from Not-yet-specified)
- ~~Whether `gh ship` should also be a pi tool~~ — RESOLVED via ticket 01 (the repurposed merge tool is the pi-tool equivalent of `gh ship`).
- "Improve devops" scope still open: PR & commit conventions (PR body template, conventional-commit enforcement, auto-link issues) and branch-hygiene automation (auto-delete merged branches, stale-branch detection) — surfaced in breadth-first grilling but not selected; remain fog.
- (Release/publish tooling graduated to ticket 03.)

## False premises (what we assumed that turned out wrong)
- 'Remote CI gates merges' — we DISABLED remote CI by design; local_ci is the local proxy gate. The effort was local-CI-first throughout.
- 'await_pr_merge should poll remote checks + enable auto-merge' — moot with CI off; it's now a local-CI-gated single-shot merge (no polling).

## Footguns (traps discovered)
- The subagent commitScope post-run detector is UNRELIABLE — false positives 4x this session (flagged 22-74 paths NOT in the actual commit). Verify any such warning with `git show <sha> --name-only`; a scoped `git add <path>` yields clean commits regardless. Don't trust the warning; don't let it block.
- Implementer commits can strand on the wrong branch (ticket 04's commit landed on a merged-base branch, fix/devops-local-ci-m3-detection). Always branch shippable work off origin/main; re-home via cherry-pick if stranded.
- runLocalCi THROWS (not returns detectionError) on an unresolvable base ref; the merge recipe wraps it fail-closed. Cleaner contract: return detectionError (a refinement, non-blocker).
- This repo path is a git WORKTREE (main in sibling worktree /Users/huangziyu/proj/video_generation); branch switch/delete from here is constrained. Multiple worktrees share one GLM API key (relevant to the rate-limit effort).
- .planning/2026-08-07-* effort dirs are UNTRACKED (working-tree only); older efforts (2026-08-02/04) are tracked. Bookkeeping edits persist uncommitted.

## Recommended next goal
Write the rate-limit wayfinder map (effort 2026-08-07-we-need-configurable-subagent-paralel-run-to-fol) — fully charted (global concurrency cap both subagents+workflow honor; ~/.pi/workflows/settings.json rateLimits map; always-on-when-configured), ready to write map.md + 4 tickets (T1 obtain GLM Pro numbers, T2 shared per-provider limiter, T3 config+loader, T4 auto-clamp). Most at-risk — currently only in conversation.

## Alternatives
- PR the TUI feature branch (feature/subagents-tui-realtime-log, 8 ahead/2 behind) to main.
- Stale-branch cleanup (fix/devops-local-ci-m3-detection, feat/devops-local-ci-tool, + merged fix branches).
- A fresh effort.
