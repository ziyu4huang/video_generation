# Next goal: none scheduled — src-entry migration complete (stale-dist class retired)

## Completed (this effort: 2026-08-15-src-entry-migration)

- **All four dist-entry packages migrated to src roots** — workflow, superpowers, wayfind,
  webui now resolve `main`/`types`/`exports["."]` to `./src/index.ts`. The stale-dist bug
  class (2026-08-15 jiti NameTooLong incident, cf6f1394) is retired at the habitat.
  PRs #1391 (webui pilot, `922664b6`), #1401 (superpowers+wayfind, `25000f35`),
  #1403 (workflow+consumers, `aeff501b`).
- **Publish face dropped by decision** (ticket 01, user-confirmed): npm registry proof —
  all four 404 (never published; `@repo` scope unownable). publishConfig/files/
  prepublishOnly deleted; workflow's upstream `repository`/`author` provenance removed.
- **Machinery retired** (ticket 05): `ensure-workspace-dist` boot heal patch + its #1370
  heal-loop tests + `BUN_PI_ENSURE_WORKSPACE_DIST` knob deleted; `workspace-dist-staleness.ts`
  slimmed 131→44 lines to `distEntryMain` alone; pi-agent's workflow postinstall heal deleted.
- **Gate flipped into a tripwire**: `bun run test:dist` now asserts ZERO `@repo/*` dist-root
  packages — a re-added one is a regression (remediation: migrate, not build). The polarity
  flip had to ride ticket 04: the old vacuity guard (`checked > 0`) fired the moment the
  fourth root flipped and blocked that very push.
- **Consumer tail swept**: boot-smoke prebuild, devops ci-matrix expectations, flux2's deep
  `dist/workflow.js` import (the last dist consumer anywhere), workflow-command.test.ts's
  mock-bypass import correctly KEPT (it dodges `mock.module`, not dist).

## Reflections (false premises / footguns)

- The four `extensions/<X>.ts` entries already imported `../src/` — the entire dist exposure
  was the package.json ROOT fields. What looked like a bundling/JITI problem was a
  package-resolution problem wearing its costume.
- Deep-import consumers are invisible until the fan-out runs: flux2's `dist/workflow.js`
  import survived three greps for `@repo/pi-agent-ext-workflow` (it used a RELATIVE path).
  Grep for the artifact path, not just the specifier, when retiring an artifact.
- A vacuity guard (`checked > 0`) has a tripwire baked into its own retirement — plan the
  polarity flip BEFORE the last instance disappears, or it blocks the migration's own PR.
- Stale compiled `.test.js` inside old dists silently inflated test counts (wayfind
  547 vs the real 500) — dist deletion was the reveal.
- `bun install` chmod's bin-registered CLI files +755 (devops CLIs kept flipping mid-PR);
  normalize before preflight, not after the abort.
- Publishing intent without an ownable scope is dead weight: the npm face had been
  unfalsifiable for months. Verify registry reality before preserving compat surface.

## Deferred prizes

- **obsidian vault-submodule byte-baseline drift** — still red on main (blocked #1391 and
  #1403 merges from passing pr-finish); regen baseline or re-point submodule.
- **archify vendored-bin env failure** — red in the other worktree's session; package
  content identical to main.
- **schema-cost baseline refresh** — pr-finish warned +20.07% (18810→22586 tokens, 61→71
  tools) on #1391; refresh via `cli tools-metrics --schema-cost --json` if intentional.
- **three-dot diff semantics** in changed-packages (moving-main two-tree inflation; owed
  from the local-ci ledger).
- Remote GitHub Actions re-enablement (test:dist rides along automatically).

## Next goal

None scheduled beyond the deferred prizes. Candidates: (1) obsidian baseline fix (unblocks
pr-finish for every future devops-fanout PR); (2) changed-packages three-dot diff; (3)
schema-cost baseline refresh.
