## Question
Which responsibilities are duplicated ACROSS packages, and who should own each? Candidates to map: hermes knowledge-search-tool.ts (455) vs zk retrieve.ts; hermes card-store.ts (674) vs zk card-format/ingest; hermes semantic-search.ts (547) vs zk semantic.ts; sqlite×3 + corruption-recovery (~3.3k) vs surreal-default reality; obsidian's runSubagentWithRetry living in tier-0 vault I/O. Output: a who-could-delegate-to-whom map with LOC + edge-direction implications (no decisions — that's ticket 04).
type: research
blocked by: (none)
claimed: research-03 (2026-08-17)

## Resolution

| # | Candidate | Overlap verdict | Evidence (file:line) | Delegation feasible? direction | LOC at stake |
|---|-----------|-----------------|----------------------|-------------------------------|--------------|
| 1. hermes knowledge-search-tool vs zk retrieve.ts | partial — layering, not duplication | knowledge-search-tool.ts:374 `kp.retrieveRecords(`; knowledge-pipeline-seam.ts:11 `readSeam("__piKnowledgePipeline")`; retrieve.ts:321 engine lives in zk; tool header "agent-facing retrieve over zk's vault-md graph" | already delegated (engine via seam); hermes keeps tool surface + semantic re-rank overlay only | 0 removable (adapter 455) |

Evidence 1: hermes tool calls `kp.retrieveRecords(...)` (knowledge-search-tool.ts:374) through the core-interface seam — same vault-md graph, zero duplicate retrieval algorithm. The 455 LOC = pi tool registration + param mapping + optional HNSW re-rank (`searchSemantic`) overlay zk doesn't own.
Evidence 2: zk retrieve.ts:321 `export async function retrieveRecords` is the sole engine; its other callers (loop.ts:178, zk-query.ts CLI) all stay inside zk/CLI.
Evidence 3: `RetrieveResult` contract imported from `@repo/pi-agent-core-interface` (knowledge-search-tool.ts:19) — protected edge respected, no hermes→zk hard import.
| 2. hermes card-store.ts vs zk card-format/ingest | namesake — different stores, no data-path duplication | card-store.ts:1-33 header "round-trip into the `memories` table… DB mirror"; card-store.ts:186/219 (`memories.md_id` rows); card-format.ts:145 readCard/ingest.ts:182 ingestRecords = vault .md truth; hermes frontmatter-codec.ts:1 "the ONE fs-free fence-split leaf" | N/A — zk owns vault md, hermes owns its DB mirror; only the ~50-LOC fence-split leaf is shareable (via core-interface) | ~50 (leaf) of 674+zk |

Evidence 1: card-store.ts header (lines 1-33) — "the knowledge-card round-trip into the `memories` table"; surreal branch stamps envelopes as free columns on `SurrealMemoryRepository`. It reads/writes DB rows (`row.md_id`, card-store.ts:186,219), never vault files.
Evidence 2: zk card-format.ts:145 `readCard(absPath)` / ingest.ts:182 `ingestRecords` are the only writers/parsers of vault card .md; zk's parseFrontmatter comes from obsidian ext (card-format.ts:2).
Evidence 3: hermes has its OWN deliberate frontmatter leaf (frontmatter-codec.ts:1-8 "replaces the 3 drifting hand-rolled copies") — a documented single-source WITHIN hermes, parallel to zk's; sole overlap is the fence-split shape (~50 LOC).
Evidence 4: card-store consumers are hermes-internal (walk-and-ingest.ts, planning-backfill.ts, stale-seam.ts, knowledge-search-tool) — no zk file imports it; stores are disjoint by design (mirror ≠ vault).
| 3. hermes semantic-search.ts vs zk semantic.ts | partial — complementary layers; ONLY the embedder/cosine leaf is a deliberate mirror | semantic-search.ts:442-470 cold path calls `kp.retrieveRecords({semantic:true})` (zk's arc via seam, source "zk-semantic"); embedder.ts:1-20 "Mirrors pi-agent-ext-knowledge-card/src/semantic.ts… drop-in compatible"; zk semantic.ts:94 JSON cosine cache `<vault>/.knowledge-semantic/` | shared leaf (Embedder type + LM Studio fetch + cosine, ~60 LOC) hoistable to core-interface; retrieval layers stay separate (hermes = warm HNSW over DB card_vectors; zk = vault JSON cosine) | ~60-100 (leaf only) of 547+181 |

Evidence 1: hermes searchSemantic warm path = embed query → surreal VectorStore.knn (HNSW) over its OWN card_vectors side-table (semantic-search.ts:12-38, 151); cold fallback calls zk's engine through the seam with `semantic:true` (semantic-search.ts:455 `kp.retrieveRecords`, hit source "zk-semantic", 470) — no duplicated retrieval algorithm over knowledge cards.
Evidence 2: the one true duplication is the embedder leaf: hermes store/surreal/embedder.ts (101 LOC) is documented as a structural mirror of zk's Embedder (same nomic-embed-text-v1.5, same LM Studio /v1/embeddings contract) "so a shared index is drop-in compatible"; card-vectors-cache.ts:59 adds a second `cosineSimilarity` mirroring zk semantic.ts:31 `cosine`.
Evidence 3: zk semantic.ts is consumed only by retrieve.ts:57 + graph-health.ts:57 (library-internal); hermes consumers: knowledge-search-tool.ts + composition/knowledge-semantic.ts — distinct surfaces, so hoisting the leaf to @repo/pi-agent-core-interface breaks no edge.
| 4. hermes sqlite surface (backend 1090 + memory-repo 969 + session-repo 852 + corruption-recovery 434) vs surreal DEFAULT | parallel backend, NOT duplication — deliberate fallback + test substrate behind one Repository contract | config.ts:112 `dbBackend: "surrealdb"` (default); stores.ts:102 `config.dbBackend ?? "sqlite"`; stores.ts:196 fell-back-to-sqlite warn path; corruption-recovery.ts:1-7 "extracted verbatim from sqlite-backend.ts… deliberately narrow deps" | keep (shrink optional): fallback + `/memory-switch-backend` runtime-hot since #772; no other package can own it | 3,345 at stake · ~0 removable short-term |

Evidence 1: surreal IS the configured default (config.ts:112) but sqlite is the runtime safety net: stores.ts:102 treats unset as sqlite, and stores.ts:196 auto-falls-back per-session with a `/memory-switch-backend` recovery hint when surreal init fails. Backend switch is runtime-hot (#772).
Evidence 2: sqlite is the de-facto test substrate: db.test.ts (40 refs), sqlite-session-repo/sqlite-memory-repo/sqlite-backend dedicated suites, repository-contract.test.ts runs BOTH backends (22 sqlite / 11 surreal refs), and integration suites (decay-eviction, id-lifecycle, birth-md-id) all run on sqlite.
Evidence 3: corruption-recovery.ts (434) is sqlite-only by design — header documents verbatim extraction from sqlite-backend (hermes-arch-06), imports RawDatabase from sqlite-backend.ts:6, sole caller sqlite-backend.ts.
Evidence 4: both backends sit behind one `MemoryRepository`/`SessionRepository` contract (repository-contract.test.ts:39 `make: () => repo + backendKind`), so the 3.3k is an alternate implementation, not copy-paste of surreal logic; deleting it removes the fallback + breaks the test substrate.
| 5. obsidian runSubagentWithRetry in tier-0 | ghost — ALREADY moved; the misplacement no longer exists | grep: zero hits in pi-agent-ext-obsidian/**; live def in pi-agent-ext-subagent/src/spawn-subagent.ts + spawn-subagent-subprocess.ts; only zk remnant is the historical comment zk-task-config.ts:7 ("was runSubagentWithRetry's toolsCsv parameter pre-migration") + one zk test | done — no action; doc claim stale (feeds 01) | 0 |

Evidence 1: `runSubagentWithRetry` has zero occurrences in obsidian today; it lives in pi-agent-ext-subagent (spawn-subagent.ts, spawn-subagent-subprocess.ts) — the tier-0 misplacement documented in KNOWLEDGE-LAYER.md was already fixed by the subagent migration.
Evidence 2: the sole zk reference is a comment in zk-task-config.ts:7 explaining tool-allowlists replaced runSubagentWithRetry's toolsCsv pre-migration — historical, not a live edge.
| 6. hermes walk-and-ingest.ts (814) vs zk ingest paths | clean separation — orchestration + DB-mirror, NOT duplicated ingest | walk-and-ingest.ts:225 `kp.ingestRecords(records, …)` + :237 `kp.healGraph(...)` via seam; header :164-165 "walk → family detect → adapt workflow-jsonl → kp.ingestRecords (zk writes vault-md) → kp.healGraph"; the 814 LOC = walk/adapt + mirrorVaultMdToStore/mirrorPlanningToStore/mirrorMemoryToStore + reconcilePlanningDeletions (hermes' own DB mirror) | N/A — zk owns the sink, hermes owns orchestration + mirror | 0 |

Evidence 1: every vault write goes through the seam (kp.ingestRecords / kp.healGraph); no card md is parsed or written directly by walk-and-ingest.
Evidence 2: the mirror trio (lines 432/609/708) + reconcilePlanningDeletions (746) are hermes-store concerns (DB mirror of vault truth), not zk logic.

### Notes
- Biggest lever: NOT cross-package redundancy — it is zk's CLI-tier-only modules (loop.ts 350 + merge.ts 337 ≈ 704 LOC, retire with their CLI commands per ticket 02) and the two leaf hoists (~110 LOC: embedder/cosine to core-interface, fence-split leaf).
- Layering health: the four packages are ALREADY well-layered post-#1556/#1571: retrieval + ingest engines correctly single-owned in zk and delegated via the seam (rows 1, 6); the sqlite 3.3k is a contracted parallel backend, not copy-paste (row 4); remaining "mirrors" are documented and deliberate (rows 2, 3). The docs are the least truthful layer (row 5 ghost + ticket 01's census).
- Total removable/hoistable/retirable LOC: ≈ 830 — 704 CLI-tier retirement (decision pending) + ~110 leaf hoists + ~17 trivially dead (INTERVIEW_PROMPT, ticket 02). Cross-package structural redundancy: ~0.
