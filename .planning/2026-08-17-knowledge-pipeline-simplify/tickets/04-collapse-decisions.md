## Question
Given the three censuses, which redundancies collapse — and into which owner? One decision per candidate with a keep/collapse/fold verdict + the structural reason.
type: grilling
blocked by: 01, 02, 03

claimed: main-grilling (2026-08-17)

## Resolution
User verdicts (2026-08-17):
- 04-a Execution shape = INTEGRATION POLISH, one effort, zero behavior change (fat-file splits explicitly rejected — they raise file count against the criterion).
- 04-b zk CLI tier = RETIRE loop.ts (350) + merge.ts (337) together with their CLI commands and tests (−704 LOC, −2 modules).
- 04-c Mirrored leaves = HOIST embedder/cosine (~60–100) + fence-split (~50) to @repo/pi-agent-core-interface; delete hermes-side mirror files (re-export where hermes imports).
- sqlite surface = KEEP (census row 4: contracted parallel backend, fallback + test substrate, ~0 removable short-term).
