# T7 — stale deploy prose

- cli-sh.ts:7 — header no longer references "the four legacy deploy modes".
- cli.ts:163 — comment drops "the deploy modes' self-injected `-ne`".

Cosmetic only; `dead-deploy-markers.test.ts` must stay green.

Status: **closed**
