---
effort: 2026-08-23-kcard-openviking-parity
created: 2026-08-23
last: 2026-08-23
status: active
---

# kcard OpenViking parity — Core-5 on s2-agent-ext-* + Bun + SurrealDB

## Destination

`bun-apps/s2-agent-ext-knowledge-card` gains the core 80% of OpenViking's capability set — typed memory model, FS-style read surface, session-commit→memory extraction loop, hierarchical (directory-recursive) retrieval, and bounded hotness decay — implemented purely on this repo's stack: s2-agent-ext-* extension architecture + Bun + SurrealDB as a **derived, rebuildable index**. The vault markdown stays the sole canonical source. The map ends by handing off to `to-spec` for the build plan.

## Context

Measured 2026-08-23 in this worktree unless noted.

- **kcard today**: pure vault-markdown zettelkasten, **zero DB** (zero `surreal` matches in package). 1925 active cards, 326 agg nodes / 4 layers (real-vault receipts, PR #1848/#1854 era). 4 tools registered in `extensions/knowledge-card.ts` (~1400 lines): `zk_card`, `zk_ask`, `zk_ingest`, `knowledge_query`. Tier ladder L0/L1/L2 pre-rendered with demote-not-truncate (63.3% token cut/card, 65 vs 178 — ticket 07, PR #1854). Loads `static` in `s2-agent.registry.yaml` (lines 159–170).
- **Retrieval baseline to beat/hold**: recall-audit kcard hit@5 17/20, MRR 0.688, journal 0/20 (harness `bun-apps/scripts/recall-audit.mjs`).
- **SurrealDB seam exists — in hermes only**: `bun-apps/s2-agent-ext-hermes-memory/src/store/surreal/` (1746 lines; dependency-free HTTP client for v3 `/sql`, per-user-db namespacing `user_<id>`/`memory`, contract tests proving sqlite↔surreal equivalence). Local SurrealDB service on `127.0.0.1:8000`.
- **OpenViking reference**: local `/Users/huangziyu/proj/OpenViking` (upstream `github.com/volcengine/OpenViking`, AGPLv3 — capability reference only, no code port; it is Python+Rust). Its measured claims: L0/L1/L2 layering = 34–91% input-token reduction; LoCoMo 80–83% vs 24–57% native. Core-5 determined by capability inventory (this session): virtual-FS read model, L0/L1/L2 layering, session extraction with LLM dedup-merge, hierarchical retrieval with score propagation (depends on the index carrying `parent_uri` + per-directory embeddings), hotness decay (`sigmoid(log1p(active_count)) * exp_decay(half_life 7d)` — ~60 lines).
- **Constraining decisions already made elsewhere** (cited, not re-decided): context-lifecycle D0 (breaking scope open in kcard), D3 (bge-m3 canonical via `embedding-leaf.ts`, re-confirmed 2026-08-23), D5/D6 (deterministic-first retrieval: no LLM intent analysis, no rerank in v1), D7 (md-git-canonical; OpenViking sidecar files already charted-and-rejected), D8 (feedback re-ranks bounded ≤±10%, must beat count baseline before default). knowledge-pipeline D04 (SurrealDB HNSW as primary vector store), D05 (3-tier md↔DB drift classification).
- **SurrealDB v3 measured (ticket 03, live probe 2026-08-23, `surrealdb-3.2.3`)**: HNSW works at 1024-dim COSINE (~886 rows/s bulk insert, p50 28 ms KNN warm); MTREE absent; KNN syntax `<|k,ef|>` only; directory recursion must be client-side per-level BFS (~40–50 ms/level; nested SurrealQL times out at depth 2); snowball FTS is AND-only per query; index rebuild is NOT reader-transparent (transient empties) → shadow-db swap or T5 fallback during regen; `/sql` body cap 1 MiB.

## Tickets

### Phase A — foundation seams

- [x] 01 — SurrealDB client ownership (CLOSED 2026-08-23 — D4–D8: client+tests+`SURREAL_DEFAULTS` extract into core-interface with injectable `onRoundTrip`; embedded-service stance (no env leaf); kcard db = `context_db` in per-user ns; hermes default backend flips to SurrealDB (sqlite = backup); embedding config centralized in `pre-load-providers.ts` via seam; detail in ticket. **BUILD of D4–D8 shipped same day** — `SurrealClient`+`SURREAL_DEFAULTS`+tests now live in core-interface `src/surreal-client.ts`; `backend-factory.ts` default = `surrealdb`; `EMBEDDING_CONFIG` (§4) published at startup via the pre-load-providers patch on the new `__piEmbeddingConfig` seam, `resolveSemanticEmbedConfig` order = seam → env → defaults)
- [x] 02 — SurrealDB index schema for kcard (CLOSED 2026-08-23 — D9–D14: single `card` table keyed by md filename stem + HNSW/FTS; `vec`+`embed_model` columns; plain `relation` table; append-only `usage` = RecallLedger store with replayed aggregates; content-hash fingerprint full shadow rebuild; D14 = every build ticket ships A/B-vs-baseline + independent reviewer; detail in ticket)
- [x] 03 — SurrealDB v3 vector/FTS capability probe (CLOSED 2026-08-23 — HNSW 1024-dim COSINE PASS, MTREE absent, KNN = `<|k,ef|>` only, recursion must be client-side BFS, FTS AND-only, rebuild not reader-transparent; detail in ticket)

### Phase B — capability build-out

- [x] 04 — typed memory model (CLOSED 2026-08-23 — D15–D18: types = frontmatter `type` values mirrored 1:1 by index `card.kind`, NOT folders/tags; map = `event`/`case`/`preference` ported + `entities`/`experience` already here, `trajectory` deferred, `identity`/`profile`/`soul` out per D1; registry = typed TS const `CARD_TYPES` extending the MERGE_OPS data pattern, no YAML layer; typed query = deterministic `type` filter param mirroring `tier`, leaf-only default; detail in ticket)
- [ ] 05 — FS-style read surface: `ls/tree/find/grep` semantics over vault + tier ladder; relation to existing tool surface (prototype, blocked by 02)
- [x] 06 — session commit → extraction loop (CLOSED 2026-08-23 — D28–D31 + build: `CARD_TYPES` registry + `src/extract.ts` loop + `zk_ingest` action=extract + shutdown lane; kcard 556/556 + typecheck; D14 = live smoke over the real journal (2-entry dry, LLM parse clean) + deterministic idempotency suite, full-scale live A/B folded back (resident 27b/12b → 132s per 2-entry call; gap recorded in the ticket); independent reviewer NO-GO-first → all findings fixed in-session with regression tests; **merged PR #1924** (scope CLEAN); detail in ticket)
- [x] 07 — hierarchical retrieval (CLOSED 2026-08-23 — D19–D22 + build amendments D9a/D9b: SurrealDB `card` index (`src/surreal-index.ts`, D13 shadow-gated swap, content-hash fingerprint, vectors reused from the model-keyed cache) + `src/hierarchical-retrieval.ts` (KNN seed lane + per-token FTS lane, α=0.18 blend, client-side BFS max-propagation γ=0.5, D18 type filter); getCardEmbeddings seam trap CLOSED (D22 per-call config resolution, env+seam now reach the cards side, unit-pinned); live contract tests vs surrealdb-3.2.3; D14 A/B on the real vault: hier 15/20 hit@5 MRR 0.604 vs baseline 17/20 MRR 0.688 — NO default switch (flat stays default, gate is ticket 09); detail in ticket)
- [ ] 08 — hotness decay port: sigmoid+exp formula, bounded ≤±10% per D8, RecallLedger as usage feed (grilling, blocked by 07)
- [x] 11 — hermes memory migration into `user_<ns>/memory` (CLOSED 2026-08-23, BUILT+EXECUTED — `scripts/db-transfer.ts` two-way insert-only reconciliation; to-surreal +394 mem/+130 sess/+131 msg rescued, to-sqlite fallback refreshed +2604/+1875/+21690; convergence dry-runs 0 both ways; D14 passed: A/B count tables + independent reviewer FIX-FIRST→all findings fixed incl. supersession fidelity UPDATE; detail in ticket)

### Phase C — gate & handoff

- [x] 09 — evaluation gate (CLOSED 2026-08-23 — D23–D27: standing `kcard-flat-vector` third arm + English set through three arms (`scripts/hier-english-eval.mjs`); NO scale arm; **gate PASSED — hier 17/20 hit@5, MRR 0.700 vs flat 17/20, 0.688; English 47/50 tie**; tuning ladder t1 stop-words → t2 body FTS lane → t6 absolute slug term (β=0.2, ov≥3) with probed-and-rejected t3/t4; γ A/B identical (propagation is not a battery mover); CI fixture smoke `__tests__/eval-gate.test.ts` 1.6s; load-then-index swap fix for the 10s timeout; tool-path default switch DEFERRED to ticket 05 per D27; detail in ticket)
- [ ] 10 — collapse to spec: hand off to `to-spec` → `to-tickets` for the build plan (task, blocked by 04–09; must fold in ticket 11's migration spec)

## Decisions

- **D1 — scope = OpenViking Core-5, nothing more.** Typed memory model, FS-style read surface, session-commit→memory extraction, hierarchical retrieval, bounded hotness. VikingBot, Web Studio, encryption, privacy configs, multi-tenancy, ovpack, watch management, cloud rerank/intent/VLM are out of scope — peripheral to this stack (s2-agent already is the agent framework; single-user local deployment). Reason: the capability inventory's 20/80 judgment; also honors the no-cloud rule and D5/D6.
- **D2 — SurrealDB is a derived, rebuildable index; vault md stays sole canonical.** Everything in SurrealDB (embeddings, hierarchy links, relations, hotness counters) regenerates from md; gitignored, hash-gated regen — same pattern as the existing model-keyed semantic JSON cache. Reason: consistent with D7 (md-git-canonical, sidecars already rejected) and knowledge-pipeline D05 tier classification; zero migration risk for 1925 cards.
- **D3 — Builds-on context-lifecycle; efforts stay independent.** This effort cites context-lifecycle D0/D3/D5/D6/D8 rather than re-deciding; its open tickets 08–17 stay there (ticket 08's auto-recall injector is the downstream consumer of this effort's retrieval surface, via the `__piKnowledgePipeline` seam). Back-link added to both maps.
- **D4 — SurrealClient → `@repo/s2-agent-core-interface`** (ticket 01, option 1): both consumers already depend on it — zero new dependency edges; `bumpRoundTrips` becomes an injectable `onRoundTrip` hook. Move set = client + unit tests + `SURREAL_DEFAULTS` const ONLY; per-user naming helpers move when ticket 02's build needs them. Reason: hermes/kcard stay decoupled (no drift-copy, no direct cross-import), and core-interface already hosts runtime leaves (`embedding-leaf.ts` precedent).
- **D5 — SurrealDB treated as an embedded local service** (ticket 01, user): fixed `127.0.0.1:8000` constant, no env-override leaf, no config-resolution layer; injectable `fetch` for tests is the only flexibility. Reason: MVP slimness — it is part of the local stack, not a standalone server.
- **D6 — kcard index = database `context_db` inside per-user namespace `user_<id>`** (ticket 01): user discriminator stays at the ns layer (per-user-db.ts tenancy rationale); hermes `memory` unchanged. Reason: same-user memory + context index adjacent under one ns; sanitization reused, not duplicated.
- **D7 — hermes default backend flips to SurrealDB; sqlite = permanent backup/escape hatch** (ticket 01, user — resolves leanrag-simplify D1 vs `backend-factory.ts` sqlite default): accepted losses = corruption recovery + `getDb` seams; ~~NO auto-migration of existing sqlite data~~ **amended 2026-08-23: ticket 11 builds the one-time migration** (user directive) — local instance already runs surrealdb with live data in `user_huangziyu/memory`. Reason: user decision; SurrealDB is the strategic store (D2 here, knowledge-pipeline D04).
- **D8 — embedding config centralized in `s2-agent/src/pre-load-providers.ts`** (ticket 01, user directive): `EMBEDDING_CONFIG` § (base derived from the lm-studio provider entry, model bge-m3), host publishes via `publishSeam` at startup, `embedding-leaf.ts` order seam → env → defaults. Reason: one file for all baked model config, without breaking the extensions-cannot-import-host layering; model choice (context-lifecycle D3) unchanged.
- **D9 — one `card` table (leaves + agg, `is_leaf`, record key = md filename stem) + FTS(title,summary) + HNSW** (ticket 02): single table = single HNSW + one-hop KNN; md↔db 1:1. **D10 — `vec` + `embed_model` columns on `card`**: model-swap A/B via shadow rebuild, not dual-model storage. **D11 — plain `relation(s,rel,o)` table, no `RELATE`**: no graph-walk consumers in v1; hermes `tagged` precedent. **D12 — `usage` append-only table IS the RecallLedger store; aggregates replayed onto `card` after rebuild**: usage has no md counterpart — md stays canonical for content, usage data survives rebuilds. **D13 — content-hash fingerprint full shadow rebuild + swap**: no intermediate states; unchanged vectors reused from the model-keyed cache. Reason (D9–D13): MVP — everything measured cheap enough to prefer the simplest consistent shape.
- **D14 — every build ticket ships an A/B test against the measured baseline + an independent reviewer subagent** (ticket 02, user): A/B formalized as the ticket 09 recall-audit gate (17/20 baseline); reviewer = the quality gate per repo dispatch discipline.
- **D15–D18 — typed memory model** (ticket 04, 2026-08-23; measured facts: OpenViking registry = 11 per-type YAMLs, 9 enabled, merge_op vocabulary IDENTICAL to kcard MERGE_OPS). **D15** types are frontmatter `type` values (index `card.kind` mirrors 1:1, no md churn), NOT vault folders — ticket 05 exposes VIRTUAL type directories over the discriminator. **D16** map: `event`/`case`/`preference` ported (`event` = add_only, ticket 06's writer), `entities`/`experience` already present, `trajectory` deferred (agent-training stage), `identity`/`profile`/`soul` OUT per D1 (SaaS user-model artifacts). **D17** registry = typed TS const `CARD_TYPES` in kcard src extending the MERGE_OPS as-data pattern — no YAML/plugin layer (that exists for third parties; a single version-controlled repo typechecks instead). **D18** typed query = deterministic `type` filter on `RetrieveOptions` mirroring `tier` (D5/D6 hold — caller passes the type; a subagent choosing a filter value via tool schema is a tool argument, not an intent-analyzer stage), leaf-only default (`is_leaf`); agg nodes reached via hierarchy, not type filters.
- **D19–D22 — hierarchical retrieval** (ticket 07, 2026-08-23). **D19** deterministic fork = posture (b)+(a): query embedding + caller-passed `type` filter, NO LLM intent analyzer; escalate to (c) only on a measured ticket-09 gap. **D20** recursion = client-side priority-queue BFS over the one `card` table: global KNN seeds leaves AND agg nodes in one hop, downward expansion via reverse `parent` links (plain index, per-level ~40–50 ms), child score = max(own seed, γ·parent), γ=0.5, id-sort tie-break; ≤3 sweeps as the convergence bound (≤4-layer tree converges in one). **D21** index build = kcard `src/surreal-index.ts`, D13 shadow-table shape: content-hash fingerprint, build `card_shadow`, recreate `card` + indexes only after every batch lands; vectors reused from the model-keyed JSON cache; `relation`/`usage` defined-but-empty (writers are tickets 06/08). **D22** the `getCardEmbeddings` seam trap closes inside 07: semantic.ts drops module-load config capture — resolution seam → env → defaults goes per-call (lazy) for `defaultEmbedder`/`lmStudioAvailable`/`getCardEmbeddings`/`embedQuery`, and the default MODEL comes from that resolution (fixes both the env trap and seam-publish timing). Flat search stays `retrieveRecords`' lane and stays DEFAULT — no switch without ticket 09.
- **D23–D26 — evaluation gate** (ticket 09 grilling, 2026-08-23, user). **D23** three-arm ablation is standing harness surface: `recall-audit.mjs` gains `kcard-flat-vector` (pure KNN, no FTS, no blend) alongside `kcard` (blend) and `kcard-hier`; the D3 English 50-question set runs through the same three arms at hit@4. **D24** NO scale arm — 2351 cards already carry the 4-layer tree and one-sweep convergence holds; revisit ~5k cards (knowledge-pipeline D03 trigger). **D25** default-switch gate: hier needs hit@5 ≥ 17/20 AND MRR ≥ 0.688 (both metrics, D8 count-baseline generalized); tie/loss → flat stays default, map records why. **D26** CI tripwire = seconds-scale fixture smoke (small corpus + `--test-embedder`) pinning the three arm code paths + gate-decision logic; the live battery stays on-demand (LM Studio + SurrealDB + full-vault embeds do not fit local_ci ≤5min). Measured grounding: battery has ZERO CJK queries, so reviewer F8's pure-CJK zero-lexical-seeds item is real-use damage but not a gate-number contributor; hier's incremental loss vs flat is exactly 2 queries.
- **D27 — gate PASSED; tool-path default switch lands in ticket 05** (ticket 09, 2026-08-23). The D25 gate was met with margin (hier 17/20 hit@5 + MRR 0.700 vs flat 17/20 + 0.688, reproduced twice; English set 47/50 tie) — the switch is AUTHORIZED. But `zk_ask`/`knowledge_query`/`zk.retrieve` consume `RetrieveResult` deeply (digest, tier-ladder rendering, count/scanned/excluded), and mapping `HierarchicalResult` into that shape IS the FS-read-surface reshape ticket 05 owns (this map's Fog of war names the tool-surface absorption question). Folding a lossy mapping into 09 would duplicate 05's work. Reason: correctness + non-duplicated scope; the gate numbers are 05's authorization to flip the default.
- **D32–D36 — FS read surface + the D27 switch executed** (ticket 05, 2026-08-23; full grounding in the ticket's Resolution). **D32** tool shape = ONE gated tool `zk_fs` with `op: ls|tree|find|grep|stat` (zk_card's one-tool-many-actions precedent; 1 gate family + 1 probe set vs 4 schemas — schema-cost discipline). **D33** vault-relative paths ARE the URIs; virtual type namespace `type/<kind>` rendered by ls/tree only (D15 holds); NO `kcard://` scheme (zero consumers). **D34** `zk_ask` NOT absorbed (LLM subagent pipeline, orthogonal to deterministic browsing per D5/D6); `knowledge_query` stays the query lane and gains the D18 `type` param. **D35** every op renders L0 by default with a `tier` promote param; `renderTier` stays the sole renderer. **D36** the D27 switch flips INSIDE `retrieveRecords` (all four consumers inherit: knowledge_query, zk.retrieve host-fn + hermes via seam, CLI): hier-first when `semantic && queryText`, leaf cards hydrate through the flat md-read path so the `RetrieveResult`/digest/tier contract is preserved; `ok:false`/empty → flat unchanged; escape hatches `hier:false` opt + `KCARD_HIER_DEFAULT=0` env; `RetrieveOptions.type` completes D18's flat side.
- **D28–D31 — session extraction loop** (ticket 06, 2026-08-23; measured on the local OpenViking clone). **D28** distill converge IS the extraction loop (no second pass): hermes journal stays capture-only (context-lifecycle D1); the distill pipeline (gate → LLM enrich → converge → supersede) gains the D17 typed contract + event writer; raw hermes auto-converge lane stays as-is (writes `hermes:*` upgrade candidates); new `zk_ingest` action `extract` runs the whole loop in one call. Reason: OpenViking is single-commit extraction; two passes would re-read the journal + need cross-pass dedup. **D29** dedup = deterministic-first, LLM on the ambiguous band (measured OpenViking `classify_memory_merge_mode`: add_only → passthrough; unique-new → no-merge; LLM only on cross-batch/multi-patch/existing-edit): kcard deterministic layer = gate 0.72 (retained deliberate mechanism, NOT a leanrag-D8 resurrection) + canonical-id upsert + mechanism-B supersede + add_only passthrough; LLM merge/delete judgment only for same-relevant active curated cards, with `event` (add_only) votes forced back to create. **D30** memory_diff equivalent = gitignored `output/kcard-extract/run-<ts>.json` (receipt precedent) — not vault md (D2). **D31** trigger = `zk_ingest` action=`extract` (on-demand) + `session_shutdown` auto-converge upgraded over fresh entries only (cursor state `.extract-state.json`); no scheduled lane (rejected: single-user local, queue machinery = D1 out of scope).

## Frontier

Ticket 06 (session commit → extraction loop) is CLOSED (D28–D31 + build shipped, 2026-08-23). **The queue head is ticket 05 (FS read surface)** — unblocked (02 closed) and it now carries D27's authorized default switch: map `HierarchicalResult` into the `RetrieveResult` shape `zk_ask`/`knowledge_query`/`zk.retrieve` consume (tier-ladder integration + virtual type dirs over the D18 type filter are the two design questions). 08 (hotness decay) follows 05 — the effort's remaining writer-side risk is gone (06's extraction loop is the `event` writer); 08's only open design question is the ≤±10% bound mechanics over the RecallLedger usage feed.

## Fog of war

- Rebuild/backfill strategy for 1925 cards + embeddings at index first-build (kp04 background-backfill-queue precedent may apply).
- Tool-surface impact: does the FS read surface absorb/reshape `zk_ask`/`knowledge_query` (tool-gating contract + hermes seam consumers must be consulted)?
- ~~Whether OpenViking's intent-analysis-free constraint (D5/D6) survives contact with multi-type memory~~ RESOLVED by D18 (ticket 04): the type is a caller-passed filter parameter, not an analyzer output.
- Scale trigger from knowledge-pipeline D03 (>5k rels / >2k cards — already at 1925 cards) may force relation-index decisions earlier.

## Cross-effort links

- `Builds-on: 2026-08-22-context-lifecycle` — D0 breaking scope, D3 embed canonical, D5/D6 deterministic retrieval, D8 bounded feedback; its ticket 08 auto-recall consumes our retrieval surface.
- `Builds-on: 2026-08-08-knowledge-pipeline` — D04 chose SurrealDB as the vector store; D05 tier classification is what D2 here instantiates for kcard.
- `Shares-decision-with: 2026-08-16-hermes-leanrag-simplify` — D1 SurrealDB-vs-sqlite default tension resolved here: this effort's D7 (ticket 01, 2026-08-23) executes the flip leanrag D1 specified; sqlite stays its backup.
