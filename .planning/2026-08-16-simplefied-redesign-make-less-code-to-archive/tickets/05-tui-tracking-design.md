---
type: prototype
blocking: 1
---

## Question

Design the TUI tracking additions as concrete mockups to react to: (a) viewer/dock Running-row format with live tokens·cost·turns + budget gauge (bar or fractional, e.g. `▮▮▮▯ 71% · 340k/480k`); (b) archive view row with turns/tier/error/fallback fields; (c) singular inline progress header upgraded with token counts + budget/wrap event lines; (d) where gauges' limit values come from (tier defaults vs calibrated table — coordinate with absorbed workstream C). Deliverable: text mockups of each surface (before → after), wired to real RunView fields.
