# C5 re-adjudication note (2026-09-15)

The as-run receipt (`C5.json`) says RED: the read-any expectation
{using-s2-agent-skills, to-spec, to-tickets, ask-matt} missed because the
model read `using-superpowers` — the METHODOLOGY router — and answered the
wayfind question correctly from the bootstrap routing table (grill →
to-spec, citing the DECIDE/SYNTHESIZE vs DESIGN/PLAN split).

Re-adjudicated PASS under the F3/D8 precedent (spwf-ab-closing): routing
through a family router IS a legitimate model-side route; the router read
plus the correct routing answer satisfies the case's intent (the model
resolves wayfind flow questions correctly).

The as-run RED receipt is preserved unmodified per PB-13. The expectation
set is widened in the map (using-superpowers added) for future runs.
