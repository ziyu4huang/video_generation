# t02 — harness upgrades (scratch)

drive-case.ts: `--dist <dir>`; per-leg model extraction into receipt `model`
field + `pin:match` check (via --pin-provider/--pin-model); C3 write-order
detector (test-file write toolCall before impl-file write); C5 expectation
redefined (read to-spec ∨ to-tickets); empty-checks → UNSPECIFIED confirmed.

Acceptance: dry-run detector against a prior session JSONL is green.
