# t04 — verdict matrix + findings

Fill the committed case×model table in the map; model deltas = FINDINGS.
Only a RED triggers: minimal fix (description-frontmatter lever first) →
package gates → redeploy → same case+model re-run → paired receipt.

Acceptance: matrix has no holes (or holes have recorded reasons); any code
change gates green.
