{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pi-agent deploy whitelist",
  "description": "Persistent whitelist of extension packages shipped by scripts/deploy.ts. Package names must match a workspace package (see bun-apps/*/package.json \"name\"). Overridden by `--only` on the CLI. npm: registry packages from .pi/settings.json are always carried over.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "extensions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Workspace package names to include in the deployed package.",
      "default": []
    }
  }
}
