# dependencies (bun install resolves at root)
node_modules

# build output (one level up, namespaced)
out
dist
*.tgz

# sourcemaps (never commit — they embed full original source)
*.map

# env / secrets
.env
.env.*

# caches
.eslintcache
.cache
*.tsbuildinfo

# OS / editor
.idea
.DS_Store
