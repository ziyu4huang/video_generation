# Provenance for upstream-skills baseline fixtures.
# Guarded by tests/skills-fidelity.test.ts (ADR-0004). This file MUST be
# non-empty — every re-baseline declares its source here.
#
# Baseline established: 2026-07-20
# Source state: post-#684 restored skills — commit 4fc140be
#   "revert(superpowers): restore skill byte-identicality (wayfind ticket 01)".
#   The 14 superpowers SKILL.md reverted to byte-identical upstream EXCEPT the
#   sanctioned #639 pi-port glue (using-superpowers/references/*.md, out of scope).
# Upstream origin: obra/superpowers (ported during the superpowers monolith
#   split; exact upstream commit SHA not pinned at baseline time — record it on
#   the next re-sync).
#
# Re-sync procedure (NEVER automatic):
#   1. Re-port the 14 SKILL.md from current upstream obra/superpowers.
#   2. bun scripts/rebaseline-upstream-skills.ts   (regenerates these fixtures)
#   3. Edit this file: set upstream-ref to the NEW commit SHA / version + date.
#   4. Review the fixture diff in the PR — confirm no repo-convention strings
#      (.planning/, goal_complete, ADR-, task_plan.md) sneaked back in.
upstream-ref: obra/superpowers @ v6.2.0 (commit 3dcbd5c4, 2026-07-23)
baselined: 2026-07-26
#
# Re-sync log:
#   2026-07-26 — subagent-driven-development/SKILL.md re-pinned to upstream v6.2.0
#   (commit 3dcbd5c4) to adopt the SDD plan-scoped workspace + resume-based fix-loop
#   redesign (obra/superpowers release v6.2.0). The other 13 SKILL.md unchanged.
#   Re-sync scope: ticket 05 of .planning/2026-07-26-adopt-upstream-sdd-reworks/.
#   pi-convention leak check passed (no .planning/, goal_complete, ADR-, task_plan.md,
#   or s2-agent strings in the new SKILL.md — pure upstream).
#   NOTE: the re-pinned SKILL.md references upstream's .superpowers/sdd/<plan-slug>/
#   paths; pi-port glue overrides those at runtime (ticket 06, done in commit 9671d84c).
#   2026-07-26 (2) — bulk re-pin the OTHER 12 SKILL.md to upstream v6.2.0 (commit
#   3dcbd5c4) to adopt the v6.2.0 skills compression sweep (recap/redundancy trim):
#   -222 lines total (TDD -51, finishing-a-branch -40, git-worktrees -35,
#   dispatching-parallel -18, verify -19, systematic-debugging -13, writing-skills
#   -10, brainstorming/receiving/requesting -8 each, executing-plans/writing-plans
#   -6 each). using-superpowers unchanged (already at v6.2.0). TDD's load-bearing
#   test-first core (RED/GREEN, write-failing-test) verified preserved. pi-convention
#   leak check clean. Suite 122/0.
#
# LOCAL-DIVERGENCES (2026-08-16, solution-extension simplification — plan
#   .planning/plans/2026-08-16-solution-extension-simplification.md, decision
#   record ADR-wayfind-0007): 5 superpowers SKILL.md carry sanctioned LOCAL
#   extension sections layered onto the upstream v6.2.0 bodies (fixtures
#   re-baselined to the merged result, so the byte-equal pin guards the MERGED
#   body, not bare upstream). An upstream re-sync MUST merge the new upstream
#   body WITH these local sections — do NOT blow them away with a naive re-port
#   (a naive re-port fails skills-fidelity.test.ts by design; rebase the local
#   sections onto the new upstream body, then re-baseline + log it here):
#     - dispatching-parallel-agents — "Pre-dispatch guardrails" + "Research as a
#       background subagent" sections (from wayfind subagent-dispatch-discipline
#       and research, both deleted 2026-08-16 — do not re-add as separate skills)
#     - systematic-debugging — "Engineering the reproduction loop" section
#       (from wayfind diagnosing-bugs, deleted 2026-08-16 — do not re-add)
#     - requesting-code-review + receiving-code-review — "The two review axes:
#       Standards vs Spec" / "Receiving two-axis feedback" sections (from
#       wayfind code-review, deleted 2026-08-16 — do not re-add)
#     - brainstorming — "When a question needs a prototype" section (from
#       wayfind prototype, deleted 2026-08-16 — do not re-add)
#     - writing-skills — generalized to all agent-consumed docs (from wayfind
#       writing-for-agents, deleted 2026-08-16 — do not re-add)
#     - writing-plans — "Entry criteria" section (repo-local addition,
#       develop-pipeline ticket 02, 2026-08-16 — do-not-drop-on-resync)
#     - executing-plans — "Entry criteria" + "Dispatch ledger" sections
#       (repo-local addition, develop-pipeline ticket 03, 2026-08-16 —
#       do-not-drop-on-resync)
#     - dispatching-parallel-agents — "Verify-child protocol" section
#       (repo-local addition, develop-pipeline ticket 04, 2026-08-17 —
#       do-not-drop-on-resync)
#
# LOCAL-DIVERGENCES addendum (2026-08-18): skills/dispatch-recovery/ is
#   repo-owned (promoted from .planning/knowledge/subagent-dispatch-empirics.md
#   via writing-skills), NOT an upstream port — intentionally unpinned by the
#   fidelity guard (absent from PORTED_SKILLS by design); do not mistake its
#   absence from upstream fixtures for drift on re-sync.
#
# LOCAL-DIVERGENCES addendum (2026-08-18, 2nd): skills/dispatch-budget-rebalance/
#   is likewise repo-owned (promoted from
#   .planning/knowledge/dispatch-budget-rebalance.md), NOT an upstream port —
#   intentionally unpinned (absent from PORTED_SKILLS by design); same
#   treatment as dispatch-recovery above.
#   SUPERSEDED 2026-08-19 (PR #1699): that skill was merged INTO dispatch-recovery
#   as its Calibration section and the directory deleted — 16 skill dirs -> 15.
#   Nothing about the pinned set changed (it was never pinned); the entry is kept
#   so a future reader does not go looking for a directory that was removed on
#   purpose.
#
# LOCAL-DIVERGENCES addendum (2026-08-18, 3rd): ext-simplification-round-2
#   (PR #1682) compressed five ported SKILL.md IN PLACE — writing-skills
#   715->298, subagent-driven-development 503->262, systematic-debugging and
#   test-driven-development trimmed under 300, executing-plans given a
#   single-source reference to skills/dispatch-recovery's budget sizing rule.
#   Intent-preserving by design: rules kept, prose tightened, examples merged,
#   no rule deleted. The fixtures were re-baselined to the COMPRESSED bodies,
#   so the byte-pin now guards those — not upstream v6.2.0's text.
#   An upstream re-sync MUST NOT naively restore the v6.2.0 bodies: re-apply the
#   compression onto the new upstream text, or accept the longer bodies as a
#   deliberate decision and record that here.
#   Backfilled 2026-08-19 — #1682 rewrote the fixtures without touching this
#   file at all, and every test stayed green. That silence is what the
#   fixtures-digest: line below now prevents.

# LOCAL-DIVERGENCES addendum (2026-08-20): develop-pipeline v2 (task 5 of
#   .planning/2026-08-20-develop-pipeline-v2/) FULLY REWROTE
#   dispatching-parallel-agents/SKILL.md in place — the manual-dispatch-centric
#   upstream body is gone; the skill now documents workflow templates
#   (execute-t1 / execute-plan) as the default engine with manual dispatch as
#   the exception path. This SUPERSEDES the two earlier
#   dispatching-parallel-agents divergence rows above ("Pre-dispatch
#   guardrails" + "Research as a background subagent" sections, "Verify-child
#   protocol" section) — their surviving content lives on inside the new body
#   as the brief contract, verify-child rule, and guardrails recap. An upstream
#   re-sync must NOT restore the v6.2.0 body or the pre-v2 local sections;
#   re-apply the pipeline v2 structure onto new upstream material only if it
#   is still relevant, and record it here.

# LOCAL-DIVERGENCES addendum (2026-08-23, portable-bun wave 2 task 5 of
#   .planning/2026-08-23-portable-bun-scripts/): systematic-debugging ONE
#   sanctioned reference edit — the HITL signal item now points at
#   `scripts/hitl-loop.template.ts` because the .sh was converted to a Bun
#   twin and deleted (same dir, same "copied, never executed" semantics; the
#   template is a copy source for human-in-the-loop reproduction loops). An
#   upstream re-sync must NOT restore the ../.sh path — re-apply the Bun twin
#   reference onto any new upstream body, or accept upstream text and record it
#   here. Divergence row: `systematic-debugging | scripts/hitl-loop.template.ts`.

fixtures-digest: sha256:c59f504bc1bd59401c0fa576388bc841ed75be45475c46d9256bc790d8ad67bb

divergence: systematic-debugging | Verify before claiming success
divergence: dispatching-parallel-agents | run_workflow

divergence: systematic-debugging | scripts/hitl-loop.template.ts

divergence: executing-plans | Ticket-order checkpoint
divergence: executing-plans | Queue boundary (effort ticket queues only)
divergence: subagent-driven-development | Ticket-queue linkage

divergence: dispatching-parallel-agents | dispatch-log

# Re-baseline log (appended by scripts/rebaseline-upstream-skills.ts):
#   2026-08-19 — backfill: record the ext-simplification-round-2 (#1682) in-place compression and tie the record to the fixtures via fixtures-digest (sha256:7eca97ecfe79…)
#   2026-08-20 — develop-pipeline v2 task 5: dispatching-parallel-agents fully rewritten (workflow templates default, manual dispatch exception); see LOCAL-DIVERGENCES addendum 2026-08-20 (sha256:ee44613fa363…)
#   2026-08-20 — tool renames 2026-08-20: workflow->run_workflow in dispatching-parallel-agents (docs/agents/extension-naming.md) (sha256:df9bf1995f2f…)
#   2026-08-21 — pi-agent→s2-agent repo-wide rename (2026-08-21): skills reference repo paths (bun-apps/s2-agent-ext-*); content is otherwise byte-equal to upstream superpowers (sha256:f9c709bc314b…)
#
# LOCAL-DIVERGENCES addendum (2026-08-21, ticket 08 land — amended by effort
#   2026-08-21-harness-streamline, phase S1):
#     - verification-before-completion — DELETED (ratified ticket 04/08 of
#       .planning/2026-08-16-simplefied-redesign-make-less-code-to-archive/:
#       already default-excluded, probe showed ~zero behavior cost). Removed
#       from SKILLS/PORTED_SKILLS + its fixture. Do NOT re-port on re-sync.
#     - brainstorming companions — visual-companion.md + scripts/ (server.cjs,
#       helper.js, frame-template.html, start/stop-server.sh) DELETED per the
#       same ticket (ungated, −1,739 lines); spec-document-reviewer-prompt.md
#       KEPT (amendment: wired as the spec-reviewer pass in S7). The SKILL.md's
#       "visual companion" prose remains (byte-pinned body untouched) — treat
#       it as describing an optional companion that no longer ships.
#     - systematic-debugging — one sanctioned line edit: the Phase-3 "Verify
#       Fix" checklist item `superpowers:verification-before-completion` pointer
#       (would dangle + fail bun-apps/tests/skill-reference.test.ts after the
#       deletion) became "Verify before claiming success — evidence first
#       (tests green, no regressions), then the claim". Merge this edit onto
#       any new upstream body on re-sync.
#   2026-08-21 — ticket 08 land (amended): delete verification-before-completion skill + fixture; cut brainstorming visual-companion.md + scripts/ (-1,739); keep spec-document-reviewer-prompt.md; systematic-debugging pointer line divergence (sha256:f3064cecb113…)
#   2026-08-21 — ticket 04 (2026-08-21-harness-streamline): seed machine-readable divergence rows for the known local divergences (sha256:f3064cecb113…)
#   2026-08-22 — 2026-08-22 — workflow→ultracode package rename (PR #1791): dispatching-parallel-agents references renamed repo sample paths (bun-apps/s2-agent-ext-ultracode/samples/*) (sha256:f6a1acca8ed4…)
#   2026-08-23 — portable-bun wave 2 task 5: hitl-loop.template.sh deleted, SKILL.md HITL item now points at scripts/hitl-loop.template.ts (bun twin copy source) (sha256:dcb561534ee2…)
#   2026-08-23 — ticket-execution-loop effort: sanctioned local divergence — ticket-order confirm gate (executing-plans) + ticket-queue next-goal linkage (subagent-driven-development); see .planning/2026-08-23-ticket-execution-loop (sha256:40bfbeb925fa…)
#   2026-08-23 — ticket-execution-loop effort: wording fix — record confirmed (not just re-chosen) execution order; ticket-number fallback for phase order; see .planning/2026-08-23-ticket-execution-loop (sha256:9e47b0c5e1f8…)
#   2026-08-25 — dispatching-parallel-agents SKILL.md dispatch-log line synced to manual-only truth (s2-agent round-2 ticket 10: workflow engine half + --effort/--tier dead paths removed from cli dispatch-log; schema is outcome-queryable only) (sha256:c59f504bc1bd…)

# LOCAL-DIVERGENCES addendum (2026-08-25): dispatching-parallel-agents'
#   "Query history first" bullet cites the LOCAL cli `dispatch-log` command,
#   synced this date to manual-only truth (s2-agent round-2 ticket 10 removed
#   the never-wired workflow engine half + --effort/--tier dead paths; the
#   schema is outcome-queryable only). Upstream's bullet has no such command —
#   a future re-port must re-apply the repo-local citation, not upstream's
#   wording.
#   2026-08-25 — record the dispatch-log divergence row (ticket 10 review: prose-only addendum was advisory; the row is machine-checked by skills-fidelity) (sha256:c59f504bc1bd…)
