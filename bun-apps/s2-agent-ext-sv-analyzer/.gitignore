node_modules/

# --- build / generated artifacts (never commit) ---

# Compiled analyzer module — regenerated by dsh-plugin/sv-analyzer/build.sh
# (mirrored here from the plugin build; same policy as the plugin's own
# plugin/wasm/). A fresh clone runs ./build.sh first, which mirrors the wasm
# into this dir before deploy/test. The tests skip the wasm layers when it is
# absent, so `bun test` stays green on a fresh clone.
wasm/
