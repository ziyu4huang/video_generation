{
  "name": "args-demo",
  "description": "Demo workflow pack — optional manifest args (topics) + the parallel() engine primitive. Proves packs carry real workflow behaviour, not just renamed single-file scripts.",
  "entry": "index.js",
  "kind": "workflow-pack",
  "engine": "s2-agent-ext-ultracode",
  "args": { "topics": ["alpha", "beta"] },
  "model": "lm-studio/google/gemma-4-12b",
  "howToRun": "Run by s2-agent-ext-ultracode tests/workflow-pack.test.ts (real-pack suite). Interactive: the `workflow` tool's name param (model defaults from this manifest)."
}
