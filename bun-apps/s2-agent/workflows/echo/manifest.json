{
  "name": "echo",
  "description": "Trivial workflow-pack smoke test — one agent() that echoes its args. Proves folder → manifest.json → engine end-to-end.",
  "entry": "index.js",
  "kind": "workflow-pack",
  "engine": "s2-agent-ext-ultracode",
  "howToRun": "Run by s2-agent-ext-ultracode tests/workflow-pack.test.ts (real-pack suite). Interactive: the `workflow` tool's name param resolves this pack via the shared resolver (workflow-pack.ts)."
}
