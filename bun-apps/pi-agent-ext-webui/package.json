{
  "name": "@repo/pi-agent-ext-webui",
  "private": true,
  "version": "0.1.0",
  "description": "Pi extension: an optional loopback webui that co-drives one AgentSession with the TUI behind an agentic mutex — renders agent output (md/HTML views, live transcript) and routes user interaction (prompts, abort, HITL presentations) back through the same session.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./src/*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "bun test",
    "test": "bun run test:unit"
  },
  "license": "MIT",
  "dependencies": {
    "marked": "^15"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typebox": "^1.3.7",
    "typescript": "^7.0.2"
  }
}
