{
  "name": "pi-schema-cost",
  "version": "0.1.0",
  "description": "Measure and rank the API schema-token cost of LLM tool definitions. Pure token-accounting + per-tool schema-cost — zero pi-agent deps. The #1 demand bucket, measurable in one command.",
  "license": "MIT",
  "keywords": ["pi-package", "schema-cost", "context-cost", "token", "llm-tools", "api"],
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "bun test __tests__/",
    "example": "bun examples/quick.mjs"
  }
}
