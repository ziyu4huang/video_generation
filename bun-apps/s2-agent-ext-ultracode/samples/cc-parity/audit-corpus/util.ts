// Audit corpus fixture (B1) — clean file, no TODO/FIXME markers.
export function slug(input: string): string {
  return input
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
