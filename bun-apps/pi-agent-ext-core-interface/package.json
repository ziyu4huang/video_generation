{
  "name": "@repo/pi-agent-ext-core-interface",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-extension contracts below every extension: SEAM_KEYS registry + publishSeam/readSeam accessors + KnowledgePipeline interface, plus the deterministic entity primitives two ADR-0001 tiers must share by value.",
  "license": "MIT",
  "keywords": ["pi-package", "seam", "contracts", "types"],
  "type": "module",
  "exports": { ".": { "types": "./src/types.d.ts", "default": "./src/index.ts" } },
  "files": ["src", "README.md"],
  "scripts": { "test": "bun test", "check": "tsc --noEmit" },
  "peerDependencies": { "@earendil-works/pi-coding-agent": "0.84.1", "typebox": "*" },
  "devDependencies": { "typescript": "^6.0.3" }
}
