{
  "name": "@repo/pi-agent-ext-core-task",
  "private": true,
  "version": "0.1.0",
  "description": "Pi extension: the task-execution cockpit — /goal (+ goal_complete tool) drives one objective to completion; todo (+ /todos) tracks in-session steps; ask_user_question is the structured option selector; plus the shared composite below-editor status widget. goal+todo share the widget + lifecycle hooks; ask_user_question is a self-contained modal (merged 2026-07-18). Unified entry core-task.ts.",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "core-task",
    "goal",
    "todo",
    "task-tracking",
    "ask-user",
    "questionnaire"
  ],
  "type": "module",
  "exports": {
    "./extensions/*": "./extensions/*",
    "./src/*": "./src/*",
    "./src/*.js": "./src/*.ts"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2",
    "typebox": "^1.3.6"
  },
  "devDependencies": {
    "@repo/pi-agent-ext-core-interface": "workspace:*",
    "@repo/pi-agent-ext-core-runtime": "workspace:*",
    "@repo/pi-agent-ext-subagent": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
