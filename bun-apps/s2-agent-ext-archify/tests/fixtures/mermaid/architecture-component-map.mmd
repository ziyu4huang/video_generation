flowchart TB
  subgraph resolver_bounded["Resolver domain"]
    gw["API gateway"]
    resolver["Resolver"]
    cache(["Resolved cache"])
  end
  subgraph media["Media domain"]
    image["Image service"]
    store(["Blob store"])
  end
  gw --> resolver
  resolver --> cache
  crop["Crop worker"] --> store
  image --> store
