{
  "name": "@repo/pi-agent-ext-power-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extension: power-tool suite. 4 diagnostic tools — inspect_context (context window breakdown by component), inspect_agent (agent-state YAML snapshot), inspect_extensions (extension health lint), inspect_pathology (retry-loop / error-storm detection) — plus schema-cost (static tool-schema token accounting).",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./extensions/*": "./extensions/*",
    "./schema-cost": "./src/schema-cost/index.ts"
  },
  "pi": {
    "extensions": ["./src/index.ts"],
    "skills": ["./skills"]
  },
  "scripts": {
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "example:schema-cost": "bun examples/schema-cost-quick.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@playwright/cli": "^0.1.17",
    "js-yaml": "latest",
    "typebox": "^1.3.7"
  },
  "devDependencies": {
    "@repo/pi-agent-ext-core-interface": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
