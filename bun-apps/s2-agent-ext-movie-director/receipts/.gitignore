# Regeneratable run artifacts (mp4/m4a/json/srt/png) produced by receipt scripts.
# The receipt .md files themselves are tracked; these large binaries are not.
# Glob catches any *-artifacts/ dir (whisper/clip/upscale/b/... next test runs).
*-artifacts/
