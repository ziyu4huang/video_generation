# --- build / generated artifacts (never commit) ---

# Rust build outputs (cargo target dir, native + wasm)
rust/target/

# Vendored zig toolchain for the C parser cross-build (downloaded by build.sh)
toolchain/

# Compiled analyzer module — regenerated by ./build.sh (38 MB unpacked).
# The plugin tarball ships it; a fresh clone runs ./build.sh first.
plugin/wasm/

# npm-packed plugin tarball
dist/

# Benchmark-generated fixtures (regenerated by test/gen-bench-fixtures.mjs)
test/.bench/

# Node
node_modules/
