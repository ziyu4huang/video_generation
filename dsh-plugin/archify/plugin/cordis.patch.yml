# dsh-archify bundle layer: activates the plugin's tools when the bundle
# is installed into a profile. `dsh plugin --profile <name> add dsh-archify`
# appends this layer automatically (the package declares dsh.bundle).
- insert:
    - id: archify
      name: dsh-archify
