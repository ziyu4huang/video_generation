# pi-lens Waive List

Accepted pi-lens diagnostics that are intentionally not addressed, with rationale.

## Swift line-length (6) — accepted

These are `line_length` (>200 chars) findings inside `Sources/ImageGenUtils/StylePrompts.swift`.

| File | Rule | Reason |
|------|------|--------|
| `Sources/ImageGenUtils/StylePrompts.swift:22,60,63,89,91,104` | `line_length` | **Verbatim VLM prompt text**, ported byte-for-byte from `python/mlx-movie-director/app/commands/caption.py` (`_STYLE_PROMPTS` + `_DEFECT_BLOCK`). Wrapping or reformatting these lines would diverge the Swift prompt from the Python source and break score/review comparability across directors (z-image vs flux2 vs python `run.py caption`). The python source ships them as single lines; Swift matches. These are string-literal *contents*, not code — a 200-col source limit does not apply to prompt semantics. |

## Markdown warnings — accepted

Same convention as `swift/z-image-director/.pi-lens-waive.md`: prose/tables in
`README.md` exceed the 80-col `MD013` limit and use padded `MD060` alignment for
readability. Documentation style preferences, not correctness.

| File | Rule | Reason |
|------|------|--------|
| `README.md` | `MD013/line-length`, `MD060/table-column-style` | Wide tables / inline code exceed 80 cols; padded alignment aids readability. |

## Rule-level note

- **`line_length`** on prompt strings: the defect-check block and JSON-schema
  examples are single logical lines in the VLM prompt (newlines would change
  semantics). Accepting them unwrapped preserves the port's audit trail against
  `caption.py`. Actionable Swift style rules (`identifier_name`,
  `function_parameter_count`, etc.) remain enforced.
