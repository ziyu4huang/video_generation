# Swift build artifacts
.build/
.swiftpm/

# Xcode
*.xcodeproj/
xcuserdata/
DerivedData/

# Regenerable Swift-port golden references (see verify_refs/README.md).
# Run python/mlx-movie-director/app/tests/gen_flux2_*_ref.py to materialize.
verify_refs/*.safetensors
verify_refs/ref_image.png

# scene/regional test-case generation outputs (regenerable via scripts/*.sh;
# large PNGs + run/caption JSON + embedded-image HTML reports — not source).
# Scripts themselves ARE tracked. Ignore the WHOLE folder (top-level *.html
# reports live here too, not only subdirs).
test_cases/
